# 多智能体对话降本增效实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在保持多轮对话体验和回答质量的前提下，通过智能路由、语义缓存、早停机制、轻量反思四个组件降低多 Agent 对话的 LLM token 消耗。

**Architecture:** 在现有 Orchestrator 的 Phase A/B/C 流程前后增加"闸门"层——Smart Router 决定调用哪些专家，Semantic Cache 复用相似问题的专家结果，Early Stop 在无冲突时跳过交叉审阅/仲裁，Light Validator 在最终输出前做低成本质检。

**Tech Stack:** Python 3.11 / FastAPI / SQLite / sentence-transformers(m3e-base) / Vue 3

---

## 文件结构

| 文件 | 职责 | 操作 |
|------|------|------|
| `backend/agent/cache.py` | 专家结果缓存，扩展语义缓存能力 | 修改 |
| `backend/agent/orchestrator.py` | 主控流程，集成 router/validator/early stop | 修改 |
| `backend/agent/router.py` | 智能路由：规则 + LLM 兜底 | 新增 |
| `backend/agent/validator.py` | 轻量反思：规则 + LLM 质检 | 新增 |
| `backend/db/config.py` | 新增配置开关默认值 | 修改 |
| `backend/rag.py` | 提供 embedding 模型给缓存使用 | 只读依赖 |
| `backend/tests/test_multi_agent_optimizer.py` | 新增单元测试 | 新增 |
| `frontend/src/components/TokenUsagePage.vue` | 展示新监控指标 | 修改 |

---

## Task 1: Semantic Cache 语义缓存

**Files:**
- Modify: `backend/agent/cache.py`
- Modify: `backend/agent/orchestrator.py:347-399`
- Create: `backend/tests/test_multi_agent_optimizer.py`

### Step 1.1: 写语义缓存测试

```python
# backend/tests/test_multi_agent_optimizer.py
import pytest
from agent.cache import ExpertCache


def test_exact_cache_hit():
    cache = ExpertCache(ttl_seconds=60, max_size=10)
    value = {"analysis": "test"}
    cache.put("query", "valuation", value)
    assert cache.get("query", "valuation") == value


def test_semantic_cache_hit():
    cache = ExpertCache(ttl_seconds=60, max_size=10, semantic_threshold=0.92)
    value = {"analysis": "test"}
    cache.put("今天白酒估值怎么样", "valuation", value)
    # 语义相近但表述不同
    result = cache.get("白酒今天估值如何", "valuation")
    assert result is not None
    assert result["analysis"] == "test"


def test_semantic_cache_miss_different_agent():
    cache = ExpertCache(ttl_seconds=60, max_size=10, semantic_threshold=0.92)
    cache.put("query", "valuation", {"analysis": "test"})
    assert cache.get("query", "risk") is None


def test_context_hash_affects_cache():
    cache = ExpertCache(ttl_seconds=60, max_size=10)
    cache.put("query", "valuation", {"analysis": "test"}, context_hash="ctx1")
    assert cache.get("query", "valuation", context_hash="ctx2") is None
```

### Step 1.2: 运行测试确认失败

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py -v`

Expected: FAIL，当前 `ExpertCache` 不支持语义缓存。

### Step 1.3: 修改 `backend/agent/cache.py` 实现语义缓存

```python
import hashlib
import time
import threading
from typing import Optional, Any


def _compute_context_hash(context: str) -> str:
    """计算上下文的短哈希。"""
    if not context:
        return ""
    return hashlib.md5(context.encode("utf-8")).hexdigest()[:16]


class ExpertCache:
    """支持精确匹配 + 语义相似匹配的专家结果缓存。"""

    def __init__(self, ttl_seconds: int = 300, max_size: int = 100, semantic_threshold: float = 0.92):
        self._cache: dict[str, tuple[Any, float]] = {}  # exact key -> (value, ts)
        self._semantic_entries: list[dict] = []         # [{agent_key, embedding, context_hash, value, ts}]
        self._ttl = ttl_seconds
        self._max_size = max_size
        self._semantic_threshold = semantic_threshold
        self._lock = threading.Lock()
        self._hits = 0
        self._misses = 0
        self._semantic_hits = 0
        self._embed_model = None

    def _get_embed_model(self):
        if self._embed_model is None:
            try:
                from rag import _get_embed_model
                self._embed_model = _get_embed_model()
            except Exception:
                self._embed_model = None
        return self._embed_model

    def _make_key(self, query: str, agent_key: str, context_hash: str = "") -> str:
        raw = f"{query}|{agent_key}|{context_hash}"
        return hashlib.md5(raw.encode("utf-8")).hexdigest()

    def _embed(self, query: str) -> Optional[list[float]]:
        model = self._get_embed_model()
        if model is None:
            return None
        try:
            import numpy as np
            vec = model.encode([query], normalize_embeddings=True)
            return vec[0].tolist()
        except Exception:
            return None

    @staticmethod
    def _cosine_similarity(a: list[float], b: list[float]) -> float:
        import numpy as np
        a_arr = np.array(a)
        b_arr = np.array(b)
        return float(np.dot(a_arr, b_arr) / (np.linalg.norm(a_arr) * np.linalg.norm(b_arr)))

    def _cleanup_expired(self):
        now = time.time()
        expired_keys = [k for k, (_, ts) in self._cache.items() if now - ts >= self._ttl]
        for k in expired_keys:
            del self._cache[k]
        self._semantic_entries = [
            e for e in self._semantic_entries if now - e["ts"] < self._ttl
        ]

    def get(self, query: str, agent_key: str, context_hash: str = "") -> Optional[Any]:
        with self._lock:
            self._cleanup_expired()
            # 1. 精确匹配
            exact_key = self._make_key(query, agent_key, context_hash)
            if exact_key in self._cache:
                value, ts = self._cache[exact_key]
                if time.time() - ts < self._ttl:
                    self._hits += 1
                    return value
                del self._cache[exact_key]

            # 2. 语义匹配
            query_emb = self._embed(query)
            if query_emb is not None:
                best = None
                best_score = 0.0
                for entry in self._semantic_entries:
                    if entry["agent_key"] != agent_key:
                        continue
                    if entry["context_hash"] != context_hash:
                        continue
                    score = self._cosine_similarity(query_emb, entry["embedding"])
                    if score > best_score:
                        best_score = score
                        best = entry
                if best and best_score >= self._semantic_threshold:
                    self._semantic_hits += 1
                    self._hits += 1
                    return best["value"]

            self._misses += 1
            return None

    def put(self, query: str, agent_key: str, value: Any, context_hash: str = ""):
        with self._lock:
            self._cleanup_expired()
            # 精确缓存
            exact_key = self._make_key(query, agent_key, context_hash)
            if len(self._cache) >= self._max_size:
                oldest_key = min(self._cache, key=lambda k: self._cache[k][1])
                del self._cache[oldest_key]
            self._cache[exact_key] = (value, time.time())

            # 语义缓存
            query_emb = self._embed(query)
            if query_emb is not None:
                self._semantic_entries.append({
                    "agent_key": agent_key,
                    "embedding": query_emb,
                    "context_hash": context_hash,
                    "value": value,
                    "ts": time.time(),
                })
                # 限制语义条目数
                if len(self._semantic_entries) > self._max_size * 2:
                    self._semantic_entries.sort(key=lambda e: e["ts"])
                    self._semantic_entries = self._semantic_entries[-self._max_size:]

    @property
    def stats(self) -> dict:
        total = self._hits + self._misses
        return {
            "hits": self._hits,
            "misses": self._misses,
            "semantic_hits": self._semantic_hits,
            "hit_rate": self._hits / total if total > 0 else 0,
            "size": len(self._cache) + len(self._semantic_entries),
        }


# Global singleton
expert_cache = ExpertCache()
```

### Step 1.4: 运行测试确认通过

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py -v`

Expected: PASS

### Step 1.5: 修改 `_execute_specialist_cached` 使用 context_hash 和配置 TTL

Modify `backend/agent/orchestrator.py:347-399`:

```python
def _execute_specialist_cached(tool_name: str, query: str,
                                cancel_event=None, prebuilt_context: str = "",
                                budget_mode: str = "normal", trace_id: str = "") -> str:
    """带缓存的专家执行。同一专家+同一上下文 5 分钟内复用。"""
    from db.config import get_config_int, get_config_float

    agent_key = tool_name.replace("consult_", "") if tool_name.startswith("consult_") else tool_name
    context_hash = hashlib.md5(prebuilt_context.encode("utf-8")).hexdigest()[:16] if prebuilt_context else ""

    # 应用配置覆盖默认缓存参数
    try:
        ttl = get_config_int("cache.ttl_minutes", 5) * 60
        threshold = get_config_float("cache.similarity_threshold", 0.92)
        expert_cache._ttl = ttl
        expert_cache._semantic_threshold = threshold
    except Exception:
        pass

    cached = expert_cache.get(query, agent_key, context_hash=context_hash)
    if cached is not None:
        logger.info(f"专家缓存命中: {tool_name}")
        cached["_cached"] = True
        return json.dumps(cached, ensure_ascii=False)

    result_str = _execute_specialist(tool_name, query,
                                     cancel_event=cancel_event,
                                     prebuilt_context=prebuilt_context,
                                     budget_mode=budget_mode,
                                     trace_id=trace_id)
    try:
        obj = json.loads(result_str)
        if "error" not in obj:
            expert_cache.put(query, agent_key, obj, context_hash=context_hash)
    except Exception:
        pass
    return result_str
```

### Step 1.6: 提交

```bash
cd /Users/xiaoyuer/projects/investment-analyzer
git add backend/agent/cache.py backend/agent/orchestrator.py backend/tests/test_multi_agent_optimizer.py
git commit -m "feat: 语义缓存支持专家结果复用，按上下文哈希隔离"
```

---

## Task 2: Early Stop 早停机制

**Files:**
- Modify: `backend/agent/orchestrator.py:623-657`
- Modify: `backend/agent/orchestrator.py:1708-1764`

### Step 2.1: 写冲突检测增强测试

Add to `backend/tests/test_multi_agent_optimizer.py`:

```python
from agent.orchestrator import detect_conflicts


def test_detect_conflicts_rating_opposite():
    results = [
        {"agent": "估值专家", "analysis": "建议买入"},
        {"agent": "风险专家", "analysis": "建议卖出"},
    ]
    conflicts = detect_conflicts(results)
    assert conflicts["detected"] is True


def test_detect_conflicts_no_conflict():
    results = [
        {"agent": "估值专家", "analysis": "当前估值合理，建议持有"},
        {"agent": "配置专家", "analysis": "仓位适中，建议持有"},
    ]
    conflicts = detect_conflicts(results)
    assert conflicts["detected"] is False


def test_detect_conflicts_action_opposite():
    results = [
        {"agent": "A", "analysis": "建议加仓 中证500(510500) 10%"},
        {"agent": "B", "analysis": "建议减仓 中证500(510500) 全部卖出"},
    ]
    conflicts = detect_conflicts(results)
    assert conflicts["detected"] is True
```

### Step 2.2: 运行测试确认失败/通过

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py::test_detect_conflicts_action_opposite -v`

Expected: 当前可能 PASS 也可能 FAIL，取决于现有 `_extract_ratings` 是否能捕获操作冲突。

### Step 2.3: 增强 `detect_conflicts` 检测操作冲突

Modify `backend/agent/orchestrator.py:623-657`，在评级冲突之外增加操作建议冲突检测：

```python
def detect_conflicts(specialist_results: list) -> dict:
    """检测专家之间的评级方向冲突和操作建议冲突。"""
    ratings = _extract_ratings(specialist_results)
    conflicts = []

    # 1. 评级方向冲突
    for i in range(len(ratings)):
        for j in range(i + 1, len(ratings)):
            r1 = ratings[i]
            r2 = ratings[j]
            if r1["rating"] == "未知" or r2["rating"] == "未知":
                continue
            if r1["rating"] == r2["rating"]:
                continue
            is_conflict = False
            for a, b in _OPPOSITE_PAIRS:
                if (r1["rating"] == a and r2["rating"] == b) or (r1["rating"] == b and r2["rating"] == a):
                    is_conflict = True
                    break
            if is_conflict:
                conflicts.append({
                    "type": "rating",
                    "expert1": r1["agent"],
                    "rating1": r1["rating"],
                    "expert2": r2["agent"],
                    "rating2": r2["rating"],
                    "fund": r1["fund"] or r2["fund"] or "",
                })

    # 2. 操作建议冲突：同一标的被给出相反操作
    action_patterns = {
        "buy": ["买入", "加仓", "定投", "增持"],
        "sell": ["卖出", "减仓", "止盈", "清仓", "减持"],
    }
    fund_code_pattern = r"\b(\d{6}|\d{5,6})\b"

    actions = []
    for sr in specialist_results:
        text = sr.get("analysis", "")
        found_codes = re.findall(fund_code_pattern, text)
        if not found_codes:
            continue
        action_type = None
        for act, keywords in action_patterns.items():
            if any(kw in text for kw in keywords):
                action_type = act
                break
        if action_type:
            for code in found_codes:
                actions.append({
                    "agent": sr.get("agent", sr.get("agent_key", "未知")),
                    "action": action_type,
                    "fund": code,
                })

    for i in range(len(actions)):
        for j in range(i + 1, len(actions)):
            a1 = actions[i]
            a2 = actions[j]
            if a1["fund"] == a2["fund"] and a1["action"] != a2["action"]:
                conflicts.append({
                    "type": "action",
                    "expert1": a1["agent"],
                    "rating1": a1["action"],
                    "expert2": a2["agent"],
                    "rating2": a2["action"],
                    "fund": a1["fund"],
                })

    return {
        "detected": len(conflicts) > 0,
        "items": conflicts,
    }
```

### Step 2.4: 修改 Orchestrator 流程控制，无冲突时跳过 Phase B/C

Modify `backend/agent/orchestrator.py:1708-1764`，把交叉审阅触发条件从"有分歧"改为"有冲突"：

```python
# Phase A 结束后检查是否需要 Phase B/C
conflicts = detect_conflicts(specialist_results)
needs_cross_review = (
    len(specialist_results) >= 2
    and not force_skip_cross_review
    and conflicts["detected"]
    and get_config("early_stop.enabled", "true") == "true"
)

if needs_cross_review:
    logger.info(f"进入交叉审阅阶段,{len(specialist_results)} 个专家参与")
    peer_analyses = {sr["agent_key"]: sr["analysis"] for sr in specialist_results}
    # ... 原有交叉审阅逻辑 ...
else:
    if len(specialist_results) >= 2:
        logger.info("专家结论一致或早停关闭，跳过交叉审阅")

# Phase C 仲裁同样只在有冲突时触发
if not arbitration_done and should_arbitrate(complexity, specialist_results, conflicts):
    # ... 原有仲裁逻辑 ...
```

同时修改 `should_arbitrate` 签名，新增 `conflicts` 参数：

```python
def should_arbitrate(complexity: str, specialist_results: list, conflicts: dict = None) -> bool:
    if get_orchestration_config("arbitration_enabled", "true") != "true":
        return False
    if conflicts is not None and not conflicts.get("detected"):
        return False
    if OrchestratorOptimizer.should_skip_arbitration(specialist_results, complexity):
        return False
    # ... 其余逻辑不变 ...
```

### Step 2.5: 运行测试确认通过

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py -v`

Expected: PASS

### Step 2.6: 提交

```bash
cd /Users/xiaoyuer/projects/investment-analyzer
git add backend/agent/orchestrator.py backend/tests/test_multi_agent_optimizer.py
git commit -m "feat: 早停机制——无冲突时跳过交叉审阅和仲裁"
```

---

## Task 3: Smart Router 智能路由

**Files:**
- Create: `backend/agent/router.py`
- Modify: `backend/db/config.py`
- Modify: `backend/agent/orchestrator.py:1585-1635`

### Step 3.1: 写路由测试

Add to `backend/tests/test_multi_agent_optimizer.py`:

```python
from agent.router import SmartRouter


def test_router_rule_match_valuation():
    router = SmartRouter()
    result = router.route("白酒估值怎么样？")
    assert "valuation" in result["specialists"]
    assert result["route_by"] == "rule"


def test_router_rule_match_portfolio():
    router = SmartRouter()
    result = router.route("我的持仓分散吗？")
    assert "diversification" in result["specialists"] or "allocation" in result["specialists"]


def test_router_mention_override():
    router = SmartRouter()
    result = router.route("随便问", target_specialists=["valuation"])
    assert result["specialists"] == ["valuation"]
    assert result["route_by"] == "mention"


def test_router_cache_does_not_duplicate():
    router = SmartRouter()
    r1 = router.route("今天大盘怎么看")
    r2 = router.route("今天大盘怎么看")
    assert r1 == r2
```

### Step 3.2: 运行测试确认失败

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py::test_router_rule_match_valuation -v`

Expected: FAIL，`SmartRouter` 不存在。

### Step 3.3: 实现 `backend/agent/router.py`

```python
"""智能路由：规则优先 + LLM 兜底，决定调用哪些专家。"""

import json
import logging
import re
import time
from typing import Optional

from db.agents import load_specialist_agents
from db.config import get_config, get_config_float
from llm_service import _call_llm, MODEL

logger = logging.getLogger(__name__)

# 关键词 -> 专家 key 列表（按优先级排序）
_KEYWORD_ROUTES = [
    (["估值", "PE", "PB", "百分位", "低估", "高估"], ["valuation"]),
    (["风险", "回撤", "止损", "亏损", "最大回撤"], ["risk"]),
    (["配置", "仓位", "股债", "比例", "再平衡"], ["allocation"]),
    (["持仓", "分散", "穿透", "集中度"], ["diversification", "allocation"]),
    (["市场", "大盘", "行情", "走势", "牛市", "熊市"], ["market"]),
    (["买", "卖", "操作", "定投", "止盈", "加仓", "减仓"], ["allocation", "risk", "valuation"]),
    (["文章", "公众号", "解读", "新闻"], ["article_analysis"]),
    (["债券", "利率", "信用", "久期"], ["bond"]),
]


class SmartRouter:
    """智能路由。规则命中零 LLM 成本；未命中时用 LLM 兜底。"""

    def __init__(self):
        self._cache = {}
        self._cache_ttl = 60  # 路由结果缓存 60 秒

    def _cache_key(self, query: str, history_summary: str, context_hash: str) -> str:
        return f"{query}|{history_summary}|{context_hash}"

    def _rule_route(self, query: str) -> Optional[dict]:
        specialists = set()
        for keywords, agents in _KEYWORD_ROUTES:
            if any(kw in query for kw in keywords):
                specialists.update(agents)
        if not specialists:
            return None
        return {
            "complexity": "simple" if len(specialists) == 1 else "medium",
            "specialists": list(specialists),
            "reason": f"关键词路由命中: {', '.join(specialists)}",
            "needs_arbitration": len(specialists) >= 2,
            "route_by": "rule",
        }

    def _llm_fallback_route(self, query: str, history_summary: str, portfolio_summary: str) -> dict:
        specialists = load_specialist_agents()
        expert_lines = []
        for key, info in specialists.items():
            if key == "arbitrator":
                continue
            expert_lines.append(f"- {key}: {info.get('name', key)} — {info.get('description', '')}")
        expert_list = "\n".join(expert_lines)

        prompt = f"""你是投资分析需求路由专家。分析用户问题，返回 JSON。

可选专家：
{expert_list}

请返回严格 JSON：
{{
  "complexity": "simple|medium|complex",
  "specialists": ["专家key1", "专家key2"],
  "reason": "简短理由",
  "needs_arbitration": true/false
}}

历史摘要：{history_summary}
持仓摘要：{portfolio_summary}
当前问题：{query}"""

        try:
            response = _call_llm(
                caller="smart_router",
                model=MODEL,
                messages=[
                    {"role": "system", "content": "你只做路由决策，不输出分析内容。"},
                    {"role": "user", "content": prompt},
                ],
                temperature=get_config_float("llm.temperature_tool", 0.2),
                max_tokens=500,
            )
            content = response.choices[0].message.content or ""
            # 尝试提取 JSON
            match = re.search(r"\{.*\}", content, re.DOTALL)
            if match:
                data = json.loads(match.group(0))
                data["route_by"] = "llm_fallback"
                return data
        except Exception as e:
            logger.warning(f"LLM 路由兜底失败: {e}")

        # 回退：返回通用配置
        return {
            "complexity": "medium",
            "specialists": ["valuation", "risk"],
            "reason": "LLM 路由失败，回退到默认专家",
            "needs_arbitration": True,
            "route_by": "llm_fallback",
        }

    def route(self, query: str, history_summary: str = "", portfolio_summary: str = "",
              target_specialists: Optional[list] = None) -> dict:
        """路由入口。"""
        # 1. 用户显式指定专家
        if target_specialists:
            all_agents = load_specialist_agents()
            valid = [s for s in target_specialists if s in all_agents]
            if valid:
                return {
                    "complexity": "simple" if len(valid) == 1 else "medium",
                    "specialists": valid,
                    "reason": f"用户通过 @mention 指定专家: {', '.join(valid)}",
                    "needs_arbitration": len(valid) >= 2,
                    "route_by": "mention",
                }

        # 2. 检查缓存
        ctx_hash = hashlib.md5((history_summary + portfolio_summary).encode("utf-8")).hexdigest()[:16]
        cache_key = self._cache_key(query, history_summary, ctx_hash)
        now = time.time()
        if cache_key in self._cache:
            cached, ts = self._cache[cache_key]
            if now - ts < self._cache_ttl:
                return cached

        # 3. 规则路由
        if get_config("router.enabled", "true") == "true":
            rule_result = self._rule_route(query)
            if rule_result:
                self._cache[cache_key] = (rule_result, now)
                return rule_result

        # 4. LLM 兜底
        if get_config("router.use_llm_fallback", "true") == "true":
            llm_result = self._llm_fallback_route(query, history_summary, portfolio_summary)
            self._cache[cache_key] = (llm_result, now)
            return llm_result

        # 5. 最终回退
        fallback = {
            "complexity": "medium",
            "specialists": ["valuation", "risk"],
            "reason": "路由关闭且未指定专家，使用默认配置",
            "needs_arbitration": True,
            "route_by": "default",
        }
        self._cache[cache_key] = (fallback, now)
        return fallback
```

### Step 3.4: 在 `orchestrator.py` 入口调用 Smart Router

Modify `backend/agent/orchestrator.py:1585-1635` 附近（需求澄清后、专家调用前）：

```python
from agent.router import SmartRouter

_router = SmartRouter()


def _build_portfolio_summary(prebuilt_context: str) -> str:
    """从预构建上下文提取前 500 字摘要。"""
    return prebuilt_context[:500] if prebuilt_context else ""


def _build_history_summary(history: list) -> str:
    """压缩历史为摘要。"""
    if not history:
        return ""
    recent = history[-3:]
    parts = []
    for msg in recent:
        role = msg.get("role", "")
        content = msg.get("content", "")[:200]
        parts.append(f"{role}: {content}")
    return "\n".join(parts)
```

然后在 `orchestrate` 和 `orchestrate_stream` 中，替换原有的 clarify_requirement / target_specialists 判断逻辑：

```python
# 在 token 预算检查之后、文章抓取之后
portfolio_summary = _build_portfolio_summary(prebuilt_context)
history_summary = _build_history_summary(history)
route_result = _router.route(query, history_summary, portfolio_summary, target_specialists=target_specialists)

specialists = route_result["specialists"]
complexity = route_result["complexity"]
refined_query = query
logger.info(f"路由结果: {route_result}")
```

### Step 3.5: 运行测试确认通过

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py -v`

Expected: PASS

### Step 3.6: 提交

```bash
cd /Users/xiaoyuer/projects/investment-analyzer
git add backend/agent/router.py backend/agent/orchestrator.py backend/tests/test_multi_agent_optimizer.py
git commit -m "feat: Smart Router 智能路由，规则优先 + LLM 兜底"
```

---

## Task 4: Light Validator 轻量反思

**Files:**
- Create: `backend/agent/validator.py`
- Modify: `backend/agent/orchestrator.py`

### Step 4.1: 写验证器测试

Add to `backend/tests/test_multi_agent_optimizer.py`:

```python
from agent.validator import LightValidator


def test_validator_passes_executable_answer():
    v = LightValidator()
    result = v.validate(
        query="白酒还能买吗？",
        final_answer="建议持有 招商白酒(161725) ，触发条件为 PE 分位低于 30% 时加仓 10%。",
        specialist_results=[{"agent": "估值专家", "analysis": "当前估值合理"}],
        context="持仓：招商白酒 1000 元",
    )
    assert result["passed"] is True


def test_validator_catches_missing_action():
    v = LightValidator()
    result = v.validate(
        query="白酒还能买吗？",
        final_answer="白酒目前估值处于历史中位数附近，投资者可根据自身情况决定。",
        specialist_results=[{"agent": "估值专家", "analysis": "估值合理"}],
        context="",
    )
    assert result["passed"] is False
    assert any("可执行" in issue for issue in result["issues"])


def test_validator_catches_hallucinated_fund():
    v = LightValidator()
    result = v.validate(
        query="白酒还能买吗？",
        final_answer="建议买入 招商白酒(161725) 。",
        specialist_results=[],
        context="持仓：易方达蓝筹 2000 元",
    )
    assert result["passed"] is False
```

### Step 4.2: 运行测试确认失败

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py::test_validator_passes_executable_answer -v`

Expected: FAIL，`LightValidator` 不存在。

### Step 4.3: 实现 `backend/agent/validator.py`

```python
"""轻量反思：最终答案输出前的低成本质检。"""

import json
import logging
import re
from typing import Optional

from db.config import get_config, get_config_float
from llm_service import _call_llm, MODEL

logger = logging.getLogger(__name__)


class LightValidator:
    """轻量验证器：规则检查为主，LLM 检查为辅。"""

    _VAGUE_PHRASES = [
        "根据自身情况",
        "仅供参考",
        "可以考虑",
        "适当调整",
        "自行判断",
    ]

    _ACTION_KEYWORDS = ["买入", "卖出", "持有", "定投", "加仓", "减仓", "止盈", "止损", "清仓"]

    def __init__(self):
        pass

    def _rule_checks(self, query: str, final_answer: str, context: str) -> list[str]:
        issues = []

        # 1. 可执行性检查
        has_action = any(kw in final_answer for kw in self._ACTION_KEYWORDS)
        has_fund_code = bool(re.search(r"\b(\d{6}|\d{5,6})\b", final_answer))
        has_amount = bool(re.search(r"\d+元|\d+%|百分之\d+|仓位\d+", final_answer))
        has_trigger = any(kw in final_answer for kw in ["当", "如果", "跌至", "涨到", "分位", "阈值"])

        if not has_action:
            issues.append("可执行性：缺少明确的操作（买/卖/持有/定投等）")
        if has_action and not has_fund_code:
            issues.append("可执行性：操作建议缺少具体基金代码")
        if has_action and not has_amount:
            issues.append("可执行性：缺少金额或比例")
        if has_action and not has_trigger:
            issues.append("可执行性：缺少触发条件")

        # 2. 模糊表述检查
        for phrase in self._VAGUE_PHRASES:
            if phrase in final_answer:
                issues.append(f"模糊表述：包含'{phrase}'")

        # 3. 数据真实性/幻觉检查：答案中的基金代码是否在上下文中
        codes_in_answer = set(re.findall(r"\b(\d{6}|\d{5,6})\b", final_answer))
        codes_in_context = set(re.findall(r"\b(\d{6}|\d{5,6})\b", context))
        # 如果答案有基金代码，但上下文也有基金代码且完全不重叠，可能幻觉
        if codes_in_answer and codes_in_context and not codes_in_answer.intersection(codes_in_context):
            issues.append(f"数据真实性：答案中的基金代码 {codes_in_answer} 未在上下文中出现")

        return issues

    def _llm_check(self, query: str, final_answer: str, specialist_results: list, context: str) -> list[str]:
        if get_config("validator.llm_check_enabled", "true") != "true":
            return []

        expert_summary = "\n".join(
            f"- {sr.get('agent', sr.get('agent_key', '专家'))}: {sr.get('analysis', '')[:200]}"
            for sr in specialist_results
        )

        prompt = f"""你是答案质检员。检查最终答案是否存在问题，只返回 JSON。

原始问题：{query}
专家结论摘要：
{expert_summary}
上下文：{context[:800]}
最终答案：{final_answer}

请检查：
1. 最终答案是否与多数专家观点矛盾
2. 是否有数据无法从上下文中找到来源
3. 是否有明显逻辑错误

返回严格 JSON：{{"issues": ["问题1", "问题2"]}}，无问题返回 {{"issues": []}}"""

        try:
            response = _call_llm(
                caller="light_validator",
                model=MODEL,
                messages=[{"role": "user", "content": prompt}],
                temperature=get_config_float("llm.temperature_tool", 0.2),
                max_tokens=400,
            )
            content = response.choices[0].message.content or ""
            match = re.search(r"\{.*\}", content, re.DOTALL)
            if match:
                data = json.loads(match.group(0))
                return data.get("issues", [])
        except Exception as e:
            logger.warning(f"LLM 质检失败: {e}")

        return []

    def validate(self, query: str, final_answer: str, specialist_results: list, context: str = "") -> dict:
        """验证最终答案。

        返回：
            {"passed": bool, "issues": list[str], "severity": "low|medium|high"}
        """
        issues = self._rule_checks(query, final_answer, context)
        issues.extend(self._llm_check(query, final_answer, specialist_results, context))

        # 去重
        issues = list(dict.fromkeys(issues))

        # 定级
        if any("可执行性" in i or "矛盾" in i for i in issues):
            severity = "high"
        elif any("数据真实性" in i or "幻觉" in i for i in issues):
            severity = "high"
        elif len(issues) >= 2:
            severity = "medium"
        elif issues:
            severity = "low"
        else:
            severity = "none"

        return {
            "passed": len(issues) == 0,
            "issues": issues,
            "severity": severity,
        }
```

### Step 4.4: 在 Orchestrator 最终答案后调用 Validator

Modify `backend/agent/orchestrator.py`，在生成最终 `answer` 之后、return 之前：

```python
from agent.validator import LightValidator

_validator = LightValidator()


def _validate_and_repair(query: str, answer: str, specialist_results: list,
                         prebuilt_context: str, llm_messages: list,
                         trace_id: str = "") -> str:
    """轻量验证并尝试修复一次。"""
    max_attempts = int(get_config("validator.max_repair_attempts", "1"))
    if max_attempts <= 0 or get_config("validator.enabled", "true") != "true":
        return answer

    for attempt in range(max_attempts):
        result = _validator.validate(query, answer, specialist_results, prebuilt_context)
        if result["passed"]:
            break
        if result["severity"] != "high":
            break
        logger.info(f"Validator 发现问题，尝试修复 (attempt {attempt + 1}): {result['issues']}")
        repair_prompt = (
            "请根据以下质检问题修复最终答案，确保建议具体、可执行、数据真实：\n"
            + "\n".join(f"- {issue}" for issue in result["issues"])
        )
        try:
            response = _call_llm(
                caller="orchestrator_repair",
                trace_id=trace_id,
                model=MODEL,
                messages=llm_messages + [
                    {"role": "assistant", "content": answer},
                    {"role": "user", "content": repair_prompt},
                ],
                temperature=get_config_float("llm.temperature_agent", 0.3),
                max_tokens=get_config_int("llm.max_tokens_orchestrator", 8192),
            )
            answer = response.choices[0].message.content or answer
        except Exception as e:
            logger.error(f"修复调用失败: {e}")
            break
    return answer
```

在 `orchestrate` 和 `orchestrate_stream` 中，生成 `answer` 后调用：

```python
answer = _validate_and_repair(
    query, answer, specialist_results, prebuilt_context,
    llm_messages, trace_id=trace_id
)
```

### Step 4.5: 运行测试确认通过

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py -v`

Expected: PASS

### Step 4.6: 提交

```bash
cd /Users/xiaoyuer/projects/investment-analyzer
git add backend/agent/validator.py backend/agent/orchestrator.py backend/tests/test_multi_agent_optimizer.py
git commit -m "feat: Light Validator 轻量反思，输出前质检 + 一次修复"
```

---

## Task 5: 配置开关与监控指标

**Files:**
- Modify: `backend/db/config.py`
- Modify: `backend/agent/orchestrator.py`
- Modify: `frontend/src/components/TokenUsagePage.vue`

### Step 5.1: 在 `db/config.py` 添加默认配置

Modify `backend/db/config.py`：在 DEFAULT_CONFIGS 列表中合适位置（如 LLM 成本控制开关后）追加：

```python
    # 多智能体对话降本增效开关
    ('router.enabled', 'true', '是否启用智能路由', 'router'),
    ('router.use_llm_fallback', 'true', '规则未命中时是否用 LLM 兜底', 'router'),
    ('router.default_complexity', 'medium', '默认复杂度', 'router'),
    ('cache.semantic.enabled', 'true', '是否启用语义缓存', 'cache'),
    ('cache.ttl_minutes', '30', '专家结果缓存 TTL（分钟）', 'cache'),
    ('cache.similarity_threshold', '0.92', '语义缓存相似度阈值', 'cache'),
    ('early_stop.enabled', 'true', '是否启用早停机制', 'early_stop'),
    ('early_stop.min_specialists', '2', '早停检查的最少专家数', 'early_stop'),
    ('validator.enabled', 'true', '是否启用轻量反思', 'validator'),
    ('validator.max_repair_attempts', '1', 'Validator 最大修复次数', 'validator'),
    ('validator.llm_check_enabled', 'true', '是否启用 LLM 质检', 'validator'),
```

### Step 5.2: 在 Orchestrator 返回结果中增加路由/缓存/早停/验证器指标

Modify `backend/agent/orchestrator.py`，在返回 `_result` 前增加：

```python
_result["route_info"] = route_result
_result["cache_stats"] = expert_cache.stats
_result["validator"] = {"enabled": get_config("validator.enabled", "true") == "true"}
```

### Step 5.3: 前端 Trace/调用明细展示新指标（可选）

Modify `frontend/src/components/TokenUsagePage.vue`：
- 在 Trace 查询结果汇总中显示 `route_by` 和 `cache_hit_rate`
- 此步骤为可选，如时间紧可跳过

### Step 5.4: 后端语法检查与全量测试

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m py_compile agent/cache.py agent/router.py agent/validator.py agent/orchestrator.py db/config.py`

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m pytest tests/test_multi_agent_optimizer.py -v`

Expected: PASS

### Step 5.5: 提交

```bash
cd /Users/xiaoyuer/projects/investment-analyzer
git add backend/db/config.py backend/agent/orchestrator.py frontend/src/components/TokenUsagePage.vue
git commit -m "feat: 新增降本增效配置开关与监控指标暴露"
```

---

## 最终验证

### 集成测试

```bash
cd /Users/xiaoyuer/projects/investment-analyzer/backend
python3 -m pytest tests/test_multi_agent_optimizer.py -v
```

### 手动验证

1. 启动后端，发起一个简单问题（如"白酒估值怎么样"）
2. 观察日志：应显示 `路由结果: ...` 和专家调用情况
3. 用相同/相似问题再问一次：应显示 `专家缓存命中`
4. 发起一个需要多个专家的问题（如"我的持仓怎么调整"）
5. 如果专家结论一致，日志应显示 `跳过交叉审阅`

### 验收标准

- [ ] 所有新增单元测试通过
- [ ] 后端 `py_compile` 无错误
- [ ] 路由规则命中率 > 70%（通过日志观察）
- [ ] 缓存命中率可观测（TokenUsagePage 或日志）
- [ ] 无冲突时跳过 Phase B/C
- [ ] Validator 不过度触发修复（<20% 对话触发修复）
