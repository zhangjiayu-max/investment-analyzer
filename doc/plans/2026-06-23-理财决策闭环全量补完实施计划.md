# 理财决策闭环全量补完 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 将 `2026-06-23-理财决策闭环与AI对话增强设计.md` 中剩余的非 MVP 项全量落地，形成对话、建议、决策、交易、复盘、画像、评测和持仓工作台的完整闭环。

**Architecture:** 后端先补齐稳定状态机和可复用服务函数：对话 assistant 消息状态统一、建议候选 schema 扩展、工具结果进入候选池、执行前检查全规则、复盘画像反哺。前端随后基于这些 API 调整 Chat 状态按钮和持仓页四区工作台，避免 UI 先行造成重复返工。

**Tech Stack:** Python 3.11、FastAPI、sqlite3、pytest、Vue 3 `<script setup>`、axios、Vite。

---

## File Structure

- Modify: `backend/db/conversations.py`
  - 增加 assistant 占位消息、状态更新、最近可恢复消息、重试消息创建等 helper。
- Modify: `backend/routers/conversations.py`
  - 使用统一状态：`queued/streaming/completed/failed/cancelled`。
  - 新增 `continue` 与 `retry` API，`resume` 只做恢复连接或继续已完成专家。
- Modify: `backend/db/decisions.py`
  - 扩展 `recommendation_candidates` 字段：`suggested_ratio`、`review_at`、`deferred_until`、`expires_at`、`dedupe_key`、`priority`、`source_snapshot_json`。
  - 增加 `dca/cash_reserve` 动作，增加延期、过期、结构化工具候选创建。
  - 执行前检查覆盖数据新鲜度、重复未复盘决策、调仓配置冲突、估值新鲜度、反方观点缺失。
  - 复盘后更新用户画像正负模式和行为偏差。
- Modify: `backend/db/__init__.py`
  - 重导出新增 helper。
- Modify: `backend/routers/decisions.py`
  - 候选延期、过期、工具候选、复盘统计分类 API。
- Modify: `backend/routers/analysis/four_pots.py`
  - 四笔钱、定投优化输出进入建议候选池。
- Modify: `backend/routers/watchlist.py`
  - 关注列表触发项生成建议候选。
- Modify: `backend/routers/portfolio.py`
  - 调仓配置/偏离入口生成候选，交易确认后尝试关联决策行动项。
- Modify: `frontend/src/api/index.js`
  - 新增 `continue/retry` 对话 API、候选延期/过期/工具候选 API。
- Modify: `frontend/src/components/chat/ChatMessage.vue`
  - 文案拆分：`恢复连接`、`继续分析`、`重新生成`。
- Modify: `frontend/src/components/ChatView.vue`
  - 接入 continue/retry API，不把三种动作混成 resume。
- Modify: `frontend/src/components/PortfolioManagement.vue`
  - 顶层四区工作台：今日概览、组合诊断、行动中心、规划工具。
  - 接入建议候选、到期复盘、待确认交易、关注触发。
- Modify: `frontend/src/components/DecisionRecordsPage.vue`
  - 候选延期、过期、更多复盘统计分类。
- Create: `backend/tests/test_conversation_execution_state.py`
- Create: `backend/tests/test_decision_full_loop.py`
- Create: `backend/tests/test_tool_candidates.py`
- Create: `frontend/src/__tests__/chat-execution-actions.test.js`

## Task 1: Conversation Execution State Machine

**Files:**
- Modify: `backend/db/conversations.py`
- Modify: `backend/routers/conversations.py`
- Modify: `backend/db/__init__.py`
- Test: `backend/tests/test_conversation_execution_state.py`

- [x] **Step 1: Write failing tests**

```python
import json

from db.conversations import (
    create_assistant_placeholder,
    create_conversation,
    create_message,
    get_latest_recoverable_assistant,
    get_messages,
    mark_message_execution_status,
    retry_assistant_message,
)


def test_assistant_placeholder_uses_queued_status(tmp_db):
    conv_id = create_conversation("测试")
    msg_id = create_assistant_placeholder(conv_id, user_message_id=11, content="排队中")

    msg = get_messages(conv_id, limit=10)[0]
    meta = json.loads(msg["metadata"])
    assert msg["id"] == msg_id
    assert msg["role"] == "assistant"
    assert meta["execution_status"] == "queued"
    assert meta["user_message_id"] == 11


def test_mark_message_execution_status_preserves_metadata(tmp_db):
    conv_id = create_conversation("测试")
    msg_id = create_assistant_placeholder(conv_id, user_message_id=12)

    ok = mark_message_execution_status(msg_id, "failed", error_message="网络异常", trace_id="abc")

    assert ok is True
    msg = get_messages(conv_id, limit=10)[0]
    meta = json.loads(msg["metadata"])
    assert meta["execution_status"] == "failed"
    assert meta["error_message"] == "网络异常"
    assert meta["trace_id"] == "abc"
    assert meta["user_message_id"] == 12


def test_latest_recoverable_assistant_includes_queued_streaming_cancelled_failed(tmp_db):
    conv_id = create_conversation("测试")
    create_message(conv_id, "user", "分析一下")
    old_id = create_assistant_placeholder(conv_id, user_message_id=1)
    mark_message_execution_status(old_id, "completed")
    new_id = create_assistant_placeholder(conv_id, user_message_id=2)
    mark_message_execution_status(new_id, "cancelled")

    item = get_latest_recoverable_assistant(conv_id)

    assert item["id"] == new_id
    assert item["execution_status"] == "cancelled"


def test_retry_assistant_message_creates_new_placeholder_and_keeps_failed_record(tmp_db):
    conv_id = create_conversation("测试")
    user_id = create_message(conv_id, "user", "继续分析")
    failed_id = create_assistant_placeholder(conv_id, user_message_id=user_id)
    mark_message_execution_status(failed_id, "failed", error_message="超时")

    retry_id = retry_assistant_message(failed_id)

    msgs = get_messages(conv_id, limit=10)
    failed = next(m for m in msgs if m["id"] == failed_id)
    retry = next(m for m in msgs if m["id"] == retry_id)
    assert json.loads(failed["metadata"])["execution_status"] == "failed"
    assert json.loads(retry["metadata"])["execution_status"] == "queued"
    assert json.loads(retry["metadata"])["retry_of_message_id"] == failed_id
```

- [x] **Step 2: Verify tests fail**

Run: `pytest backend/tests/test_conversation_execution_state.py -q`

Expected: import errors for missing helper functions.

- [x] **Step 3: Implement DB helpers**

Add helpers in `backend/db/conversations.py`:

- `VALID_EXECUTION_STATUSES`
- `_load_metadata(value)`
- `create_assistant_placeholder(...)`
- `mark_message_execution_status(...)`
- `get_latest_recoverable_assistant(...)`
- `retry_assistant_message(...)`

- [x] **Step 4: Export helpers**

Add exports in `backend/db/__init__.py`.

- [x] **Step 5: Verify tests pass**

Run: `pytest backend/tests/test_conversation_execution_state.py -q`

Expected: PASS.

## Task 2: Conversation Resume / Continue / Retry APIs

**Files:**
- Modify: `backend/routers/conversations.py`
- Modify: `frontend/src/api/index.js`
- Modify: `frontend/src/components/chat/ChatMessage.vue`
- Modify: `frontend/src/components/ChatView.vue`
- Test: `frontend/src/__tests__/chat-execution-actions.test.js`

- [x] **Step 1: Backend API behavior**

Add:

- `POST /api/conversations/{conv_id}/continue`
- `POST /api/conversations/{conv_id}/retry-message/{message_id}`
- `GET /api/conversations/{conv_id}/execution-state`

Rules:

- `resume` reconnects to `queued/streaming/resuming` or continues same assistant if background is gone.
- `continue` uses same assistant message and skips completed experts.
- `retry-message/{message_id}` creates a new assistant placeholder linked to failed/cancelled message.

- [x] **Step 2: Frontend API**

Add `continueConversationStream()`, `retryConversationMessageStream()`, `getConversationExecutionState()`.

- [x] **Step 3: Frontend message actions**

`ChatMessage.vue` must show:

- `streaming/resuming`: `恢复连接`
- `cancelled`: `继续分析` and `重新生成`
- `failed/timeout`: `重新生成`

- [x] **Step 4: Verify frontend action tests**

Run: `npm --prefix frontend test -- chat-execution-actions`

Expected: PASS.

## Task 3: Full Recommendation Candidate Schema

**Files:**
- Modify: `backend/db/decisions.py`
- Modify: `backend/db/__init__.py`
- Modify: `backend/routers/decisions.py`
- Test: `backend/tests/test_decision_full_loop.py`

- [x] **Step 1: Write failing tests**

Tests must verify:

- Candidate supports `suggested_ratio`, `review_at`, `deferred_until`, `expires_at`, `dedupe_key`, `priority`.
- Duplicate source/target/action/dedupe key returns existing candidate instead of creating duplicates.
- Candidate can be deferred and expired.
- `dca` and `cash_reserve` can be saved as decisions.

- [x] **Step 2: Implement schema migration and functions**

Use `_add_column_if_not_exists` pattern or `ALTER TABLE` guarded by try/except inside `init_decision_tables`.

Add:

- `defer_recommendation_candidate(candidate_id, deferred_until)`
- `expire_recommendation_candidates(user_id="default")`
- `create_candidate_from_structured_recommendation(payload)`
- richer `create_decision_from_candidate()`

- [x] **Step 3: Add routes**

Add:

- `POST /api/recommendation-candidates/{candidate_id}/defer`
- `POST /api/recommendation-candidates/expire`
- `POST /api/recommendation-candidates/from-tool`

- [x] **Step 4: Verify tests**

Run: `pytest backend/tests/test_decision_full_loop.py -q`

Expected: PASS.

## Task 4: Tool Outputs Enter Candidate Pool

**Files:**
- Modify: `backend/routers/analysis/four_pots.py`
- Modify: `backend/routers/watchlist.py`
- Modify: `backend/routers/portfolio.py`
- Test: `backend/tests/test_tool_candidates.py`

- [x] **Step 1: Write failing tests**

Tests must verify:

- Four-pots advice can create `cash_reserve` or `rebalance` candidate.
- DCA optimization can create `dca` candidate with suggested amount.
- Watchlist trigger can create `watch/add` candidate.
- Rebalance config drift can create `rebalance` candidate.

- [x] **Step 2: Implement helper calls**

Use `create_candidate_from_structured_recommendation()` from tool routers.

- [x] **Step 3: Verify tests**

Run: `pytest backend/tests/test_tool_candidates.py -q`

Expected: PASS.

## Task 5: Execution Precheck Full Rules and Review Learning

**Files:**
- Modify: `backend/db/decisions.py`
- Modify: `backend/db/dashboard.py`
- Test: `backend/tests/test_decision_full_loop.py`

- [x] **Step 1: Write failing tests**

Tests must verify:

- Missing counter arguments creates warning.
- Stale evidence creates warning.
- Existing unreviewed same-target decision creates warning.
- Active rebalance config conflict creates warning.
- Review updates `positive_patterns` / `negative_patterns` / `behavior_biases`.

- [x] **Step 2: Implement full checks**

Extend `build_decision_precheck()` and `record_decision_review()`.

- [x] **Step 3: Verify tests**

Run: `pytest backend/tests/test_decision_full_loop.py -q`

Expected: PASS.

## Task 6: Frontend Portfolio Workbench and Candidate Actions

**Files:**
- Modify: `frontend/src/api/index.js`
- Modify: `frontend/src/components/PortfolioManagement.vue`
- Modify: `frontend/src/components/DecisionRecordsPage.vue`

- [x] **Step 1: Add API functions**

Add candidate defer/expire/tool helpers and execution-state helpers.

- [x] **Step 2: Add top-level portfolio workbench**

At the top of `PortfolioManagement.vue`, add four dense work sections:

- 今日概览
- 组合诊断
- 行动中心
- 规划工具

Wire them to existing page sections, not a marketing landing block.

- [x] **Step 3: Add action center data**

Show:

- Recommendation candidates.
- Pending decisions.
- Pending transactions.
- Due reviews.
- Watchlist trigger count.

- [x] **Step 4: Add candidate defer/expire buttons**

Use existing `ConfirmDialog`.

- [x] **Step 5: Verify frontend**

Run:

- `npm --prefix frontend test -- api-urls`
- `npm --prefix frontend test -- chat-execution-actions`
- `npm --prefix frontend run build`

Expected: PASS.

## Task 7: Final Verification and Commit

- [x] **Step 1: Run backend tests**

```bash
pytest backend/tests/test_conversation_execution_state.py backend/tests/test_decision_full_loop.py backend/tests/test_tool_candidates.py backend/tests/test_recommendation_candidates.py backend/tests/test_db_decisions_phase3.py backend/tests/test_db_decisions_phase1.py backend/tests/test_conversation_context.py backend/tests/test_db_portfolio.py -q
```

- [x] **Step 2: Run frontend tests/build**

```bash
npm --prefix frontend test -- api-urls
npm --prefix frontend test -- chat-execution-actions
npm --prefix frontend run build
```

- [x] **Step 3: Diff checks**

```bash
git diff --check
git status --short
```

- [x] **Step 4: Commit**

```bash
git add doc/plans/2026-06-23-理财决策闭环全量补完实施计划.md backend frontend
git commit -m "feat: complete wealth decision loop full design"
```

## Self Review

- Spec coverage: covers all items previously identified as incomplete: conversation status semantics, continue/retry, full recommendation schema, tool integrations, guardrails, review learning, and portfolio workbench.
- Placeholder scan: no TBD/TODO placeholders remain.
- Scope check: this is intentionally full-scope because the user explicitly requested all remaining design items, not MVP.
