
准测， 后端代码 禁止写任何硬编码数据，在代码中，尤其是指标数据， 判断比例等， 必须是以配置的形式透出支持配置，让用户感知 ，防止硬编码数据，带啦 分析过程的不准确，导致差异性扩大

2、所有大模型的调用， 必须以agent的形式配置， 做到可观测，可以查询的

3、所有分析数据 一定要把当前持仓数据 和 亏损比带入 这样能够更好地 了解当前信息，利于分析

4、所有数据 必须有依据， 禁止编纂假数据，假的指标

# 每日看板

重点： 每日看板目前的模块， 每天的市场情报概览。 四大板块， 近期低估值数据、持仓的健康度分析，今日的 热门机会，和零钱配置 。 可以对自己持仓和市场等有个全面了解

1、市场日报分析师，prompt需要优化 ，市场速览看不出近期市场的行情

2、四个卡片长宽固定住，不要随着内容变宽

3、**`<font style="color:rgb(15, 23, 42);">`持仓健康度 使用的是全景分析师， 把`</font>`****`<font style="color:rgb(99, 102, 241);">`调仓分析`</font>`****`<font style="color:rgb(15, 23, 42);">` 去掉， 感觉没有用， 保留全景诊断，这里需要设计下`</font>`**
4、**`<font style="color:rgb(15, 23, 42);">`今日热门机会 使用的是 热点分析agent ， 需要的是热门的基金 今日涨幅多的 大家讨论多的， 适合 买短期的 蹭个热度那种`</font>`**

**`<font style="color:rgb(15, 23, 42);">`5、零钱配置 使用的是全景分析师agent , 要充分利用债券温度， 近3月的温度市场  结合持仓来充分分析 零钱 如何买 债券 还是买短债，货币等`</font>`**

# `<font style="color:rgb(15, 23, 42);">`AI对话`</font>`

支持和AI对话 ，每个任务会记住对话的上下文，支持对话内容压缩，滑动窗口优化上下文上限问题，防止失忆。 所有对话上下文都要记得我们当前的持仓和盈亏。  给出更专业的 回答，如果是agnet提示词 或者协作逻辑需要调整 给出优化点  。当前问题 分析中的时候， 切换页面会出现 **`<font style="color:rgb(146, 64, 14);background-color:rgb(254, 243, 199);">` 执行中断（切页面或刷新导致） 。对话ID: 13 有问题`</font>`**

排查下 每个anget执行是否有bug,  验证数据 使用， 我想买机器人相关基金。

# `<font style="color:rgb(15, 23, 42);">`估值数据`</font>`

AI 市场分析那个按钮 鼠标放上去 没有 具体agent 信息 ，要和 每日看板那里一样， 分析的按钮 鼠标放上去会有对应agent信息

# `<font style="color:rgb(15, 23, 42);">`持仓管理`</font>`

交易行为分析 那个tab页 需要一个  `<font style="color:rgb(15, 23, 42);">`交易复盘分析师 agent ,目前已经有了，但是这个tab页面没有接入， 需要接入`</font>`

# token 用量

token用量数据 全部清空。 **`<font style="color:rgb(148, 163, 184);">`调用方 最好是能够识别是哪个agent, 后面好分析 用量统计，`</font>`**

# **`<font style="color:rgb(15, 23, 42);">`进化系统`</font>`**

目前还缺少很多 用户反馈数据 所以还没有任何提升， 以及 也没有 评测集， 无法分析 这块也是重点优化的
