# 对话系统与 AI 分析增强 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复对话执行稳定性（取消/失败/重复执行）+ 注入持仓上下文与定投规则，提高理财决策准确性。

**Architecture:** DB schema 迁移（cancel_requested + run_phase）→ 后端取消/恢复语义重设计 → 前端失败状态显式化 → 防重复执行过滤 → 持仓/定投上下文注入。LLM 降本优化（conflict_detect 缓存、cross_review 单轮、ReAct 压缩）已在代码中实现，仅需更新 DB 配置值。

**Tech Stack:** Python 3.11 / FastAPI / SQLite / Vue 3 Composition API

**设计稿**: [2026-07-06-对话与AI分析增强-design.md](./2026-07-06-对话与AI分析增强-design.md)

---

## 代码现状（已验证）

| 设计稿项 | 代码状态 | 说明 |
|---------|---------|------|
| P0-2.1 conflict_detect 缓存 | ✅ 已实施 | [orchestrator.py:3944](file:///Users/xiaoyuer/projects/investment-analyzer/backend/agent/orchestrator.py) `_detect_conflicts_cached()` |
| P0-2.2 cross_review 单轮 | ✅ 已实施 | [orchestrator.py:4021-4030](file:///Users/xiaoyuer/projects/investment-analyzer/backend/agent/orchestrator.py) `run_cross_review_opinion()` |
| P0-2.3 ReAct 压缩 | ✅ 已实施 | [multi_agent.py:21-77](file:///Users/xiaoyuer/projects/investment-analyzer/backend/agent/multi_agent.py) `_compress_prior_tool_messages()` |
| P0-3.1 跨对话复用 | ✅ 代码已开启 | [config.py:155](file:///Users/xiaoyuer/projects/investment-analyzer/backend/db/config.py) `agent.reuse_recent_conclusions='true'` |
| P0-3.5 综合 prompt 增强 | ✅ 已实施 | [orchestrator.py:470-510](file:///Users/xiaoyuer/projects/investment-analyzer/backend/agent/orchestrator.py) 已含减仓约束/用户视角/数据时效 |
| 配置开关 | ✅ 代码已加 | [config.py:163-167](file:///Users/xiaoyuer/projects/investment-analyzer/backend/db/config.py) 4 个新开关 |
| P0-1.1 取消语义 | ❌ 未实施 | cancel 接口只设内存 event，不持久化 |
| P0-1.2 失败状态 UI | ❌ 未实施 | 前端不处理 failed 状态 |
| P0-1.3 防重复执行 | ❌ 未实施 | 无 run_phase 字段 |
| P0-2.4 模型路由一致性 | ❌ 未修复 | arbitrator_model 配置为 mimo，应为 deepseek |
| P0-3.2 持仓上下文注入 | ❌ 未实施 | 无 `_build_portfolio_context()` |
| P0-3.3 4% 定投法注入 | ❌ 未实施 | 无 `_build_dca_rules()` |
| DB 配置值同步 | ❌ 未做 | INSERT OR IGNORE 不更新已有 key |

---

## 文件结构

| 文件 | 责任 | 改动类型 |
|------|------|---------|
| `backend/db/__init__.py` | init_db 建表 + 迁移 | 修改：加字段 |
| `backend/db/config.py` | 配置默认值 | 修改：arbitrator_model 默认值 |
| `backend/db/conversations.py` | 对话 CRUD | 新增：cancel 相关函数 |
| `backend/db/agents.py` | agent_runs CRUD | 修改：run_phase 过滤 |
| `backend/routers/conversations.py` | cancel/resume API | 修改：cancel 持久化、resume 不自动恢复 |
| `backend/agent/orchestrator.py` | 编排逻辑 | 修改：防重复；新增：持仓/定投上下文 |
| `frontend/src/components/ChatView.vue` | 对话 UI | 修改：失败/取消状态显示 |

---

## Task 1: DB Schema 迁移 — 添加 cancel_requested 和 run_phase 字段

**Files:**
- Modify: `backend/db/__init__.py:214` (init_db 函数内)
- Test: `backend/tests/test_schema_migration.py` (新建)

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_schema_migration.py
"""测试 DB schema 迁移：cancel_requested 和 run_phase 字段。"""
import sqlite3
import pytest
from db._conn import DB_PATH


def test_conversations_has_cancel_requested_column():
    """conversations 表应有 cancel_requested 字段（默认 0）。"""
    conn = sqlite3.connect(str(DB_PATH))
    cursor = conn.execute("PRAGMA table_info(conversations)")
    columns = [row[1] for row in cursor.fetchall()]
    conn.close()
    assert "cancel_requested" in columns, f"conversations 缺少 cancel_requested 字段: {columns}"


def test_agent_runs_has_run_phase_column():
    """agent_runs 表应有 run_phase 字段（默认 'primary'）。"""
    conn = sqlite3.connect(str(DB_PATH))
    cursor = conn.execute("PRAGMA table_info(agent_runs)")
    columns = [row[1] for row in cursor.fetchall()]
    conn.close()
    assert "run_phase" in columns, f"agent_runs 缺少 run_phase 字段: {columns}"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python -m pytest tests/test_schema_migration.py -v`
Expected: FAIL with "conversations 缺少 cancel_requested 字段"

- [ ] **Step 3: 实施迁移**

在 `backend/db/__init__.py` 的 `init_db()` 函数中，找到 conversations 建表语句（约 362 行）之后，添加迁移：

```python
# backend/db/__init__.py init_db() 中，conversations 建表后添加：
_add_column_if_not_exists(conn, "conversations", "cancel_requested", "INTEGER DEFAULT 0")
```

找到 agent_runs 建表语句（约 438 行）之后，添加迁移：

```python
# backend/db/__init__.py init_db() 中，agent_runs 建表后添加：
_add_column_if_not_exists(conn, "agent_runs", "run_phase", "TEXT DEFAULT 'primary'")
```

- [ ] **Step 4: 运行迁移并验证测试通过**

Run: `cd backend && python -c "from db import init_db; init_db()" && python -m pytest tests/test_schema_migration.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/db/__init__.py backend/tests/test_schema_migration.py
git commit -m "feat(db): 添加 conversations.cancel_requested 和 agent_runs.run_phase 字段"
```

---

## Task 2: DB 配置值强制同步 — 修复陈旧配置

**Files:**
- Modify: `backend/db/config.py:117` (arbitrator_model 默认值)
- Create: `backend/scripts/sync_config.py` (一次性同步脚本)

- [ ] **Step 1: 修正 arbitrator_model 默认值**

在 `backend/db/config.py` 的 `DEFAULT_CONFIGS` 中，修改第 117 行：

```python
# 修改前：
('cost_routing.arbitrator_model', 'mimo-v2.5-pro', '仲裁模型', 'cost_routing'),
# 修改后（硬约束：仲裁法官必须用 deepseek-v4-pro）：
('cost_routing.arbitrator_model', 'deepseek-v4-pro', '仲裁模型（硬约束：必须 deepseek）', 'cost_routing'),
```

- [ ] **Step 2: 创建配置同步脚本**

```python
# backend/scripts/sync_config.py
"""强制更新陈旧的 system_config 值（INSERT OR IGNORE 不会更新已有 key）。"""
import sys
import os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from db._conn import _get_conn

# 需要强制更新的配置（key, value）
FORCE_UPDATE = [
    ('agent.reuse_recent_conclusions', 'true'),
    ('agent.reuse_conclusions_hours', '48'),
    ('agent.conflict_detect_cache', 'true'),
    ('agent.cross_review_opinion_mode', 'true'),
    ('agent.react_tool_result_max_chars', '1500'),
    ('agent.react_compress_history', 'true'),
    ('cost_routing.arbitrator_model', 'deepseek-v4-pro'),
]

def main():
    conn = _get_conn()
    for key, value in FORCE_UPDATE:
        cur = conn.execute("UPDATE system_config SET value = ? WHERE key = ?", (value, key))
        if cur.rowcount > 0:
            print(f"✅ 已更新 {key} = {value}")
        else:
            print(f"⚠️  {key} 不存在（将在 init_default_configs 时创建）")
    conn.commit()
    conn.close()

if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 运行同步脚本并验证**

Run: `cd backend && python scripts/sync_config.py && python -c "from db.config import get_config; print('reuse:', get_config('agent.reuse_recent_conclusions')); print('arbitrator:', get_config('cost_routing.arbitrator_model'))"`
Expected: 输出 `reuse: true` 和 `arbitrator: deepseek-v4-pro`

- [ ] **Step 4: 提交**

```bash
git add backend/db/config.py backend/scripts/sync_config.py
git commit -m "fix(config): 强制同步陈旧配置值，修正 arbitrator_model 为 deepseek-v4-pro"
```

---

## Task 3: 后端取消接口增强 — 持久化 cancel_requested

**Files:**
- Modify: `backend/db/conversations.py` (新增 cancel CRUD)
- Modify: `backend/routers/conversations.py:167-196` (cancel 接口增强)
- Test: `backend/tests/test_cancel_persist.py` (新建)

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_cancel_persist.py
"""测试取消标记持久化。"""
import pytest
from db.conversations import create_conversation, mark_conversation_cancelled, get_conversation_cancel_status


def test_cancel_mark_persists():
    """mark_conversation_cancelled 应写入 cancel_requested=1。"""
    conv_id = create_conversation("test cancel persist")
    try:
        assert get_conversation_cancel_status(conv_id) is False  # 初始未取消
        mark_conversation_cancelled(conv_id)
        assert get_conversation_cancel_status(conv_id) is True  # 取消后标记为 True
    finally:
        from db.conversations import delete_conversation
        delete_conversation(conv_id)
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python -m pytest tests/test_cancel_persist.py -v`
Expected: FAIL with "cannot import name 'mark_conversation_cancelled'"

- [ ] **Step 3: 在 conversations.py 新增 cancel CRUD**

在 `backend/db/conversations.py` 的 `delete_conversation` 函数（约 86 行）之后添加：

```python
def mark_conversation_cancelled(conv_id: int):
    """持久化取消标记到 DB（不依赖内存 cancel_event，刷新页面后仍生效）。"""
    conn = _get_conn()
    try:
        conn.execute("UPDATE conversations SET cancel_requested = 1 WHERE id = ?", (conv_id,))
        conn.commit()
    finally:
        conn.close()


def get_conversation_cancel_status(conv_id: int) -> bool:
    """查询对话是否被用户主动取消。"""
    conn = _get_conn()
    try:
        row = conn.execute("SELECT cancel_requested FROM conversations WHERE id = ?", (conv_id,)).fetchone()
        return bool(row and row["cancel_requested"])
    finally:
        conn.close()


def clear_conversation_cancel_flag(conv_id: int):
    """清除取消标记（用户主动重试时调用）。"""
    conn = _get_conn()
    try:
        conn.execute("UPDATE conversations SET cancel_requested = 0 WHERE id = ?", (conv_id,))
        conn.commit()
    finally:
        conn.close()
```

- [ ] **Step 4: 增强 cancel 接口**

修改 `backend/routers/conversations.py` 的 `cancel_conversation_execution` 函数（约 167 行），在 "2. 标记 DB 状态为 cancelled" 之前插入：

```python
# 在 cancel_conversation_execution 函数中，第 178 行后添加：
    # 1.5 持久化取消标记（刷新页面后仍能识别，防止自动 resume）
    try:
        from db.conversations import mark_conversation_cancelled
        mark_conversation_cancelled(conv_id)
        logger.info(f"取消对话 {conv_id}：已持久化 cancel_requested 标记")
    except Exception as e:
        logger.warning(f"持久化取消标记失败: {e}")
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd backend && python -m pytest tests/test_cancel_persist.py -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/db/conversations.py backend/routers/conversations.py backend/tests/test_cancel_persist.py
git commit -m "feat(cancel): 持久化 cancel_requested 标记，防止刷新后自动重试"
```

---

## Task 4: 后端 Resume 接口修复 — 不自动恢复已取消/失败的对话

**Files:**
- Modify: `backend/routers/conversations.py:200-260` (resume 接口)
- Test: `backend/tests/test_resume_guard.py` (新建)

**问题根因**: [routers/conversations.py:224](file:///Users/xiaoyuer/projects/investment-analyzer/backend/routers/conversations.py) 检查 `execution_status in ("streaming", "cancelled", "failed")`，导致取消/失败的对话在刷新页面时被自动 resume。取消的对话不应自动恢复，失败的需要用户显式点击重试。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_resume_guard.py
"""测试 resume 接口的取消/失败守卫。"""
import pytest
from fastapi.testclient import TestClient
from db.conversations import create_conversation, create_message, mark_conversation_cancelled


def test_cancelled_conversation_not_auto_resumed():
    """已取消的对话不应自动 resume，应返回 409 Conflict。"""
    from app import app
    client = TestClient(app)
    conv_id = create_conversation("test resume guard")
    try:
        create_message(conv_id, "user", "测试问题")
        create_message(conv_id, "assistant", "⏳ 分析进行中...",
                       metadata='{"execution_status": "streaming"}')
        mark_conversation_cancelled(conv_id)
        # 刷新页面触发 resume
        resp = client.post(f"/api/conversations/{conv_id}/resume")
        assert resp.status_code == 409, f"已取消的对话不应自动恢复: {resp.status_code}"
        assert "已取消" in resp.json()["detail"]
    finally:
        from db.conversations import delete_conversation
        delete_conversation(conv_id)
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python -m pytest tests/test_resume_guard.py -v`
Expected: FAIL（当前 resume 接口不检查 cancel_requested）

- [ ] **Step 3: 修改 resume 接口添加守卫**

在 `backend/routers/conversations.py` 的 `resume_conversation` 函数（约 200 行）中，在 `conv = get_conversation(conv_id)` 之后（约 204 行后）插入：

```python
# 在 resume_conversation 函数中，conv 存在性检查之后添加：
    # 守卫1：已取消的对话不自动恢复（用户主动取消后刷新页面，应看到取消状态而非自动重试）
    from db.conversations import get_conversation_cancel_status
    if get_conversation_cancel_status(conv_id):
        logger.info(f"恢复对话 {conv_id}：检测到 cancel_requested=true，拒绝自动恢复")
        raise HTTPException(409, "对话已被用户取消，不会自动恢复。如需重试请点击重试按钮。")

    # 守卫2：失败状态的对话不自动恢复（需要用户显式点击重试）
    # 检查最后一条 assistant 消息是否为 failed 状态
    msgs_check = get_messages(conv_id, limit=5)
    for msg in reversed(msgs_check):
        if msg["role"] == "assistant":
            meta = msg.get("metadata")
            if isinstance(meta, str):
                try:
                    import json as _json
                    meta = _json.loads(meta)
                except Exception:
                    meta = {}
            if isinstance(meta, dict) and meta.get("execution_status") == "failed":
                logger.info(f"恢复对话 {conv_id}：最后一条 assistant 消息为 failed，拒绝自动恢复")
                raise HTTPException(409, "对话上次执行失败，不会自动恢复。如需重试请点击重试按钮。")
            break  # 只检查最后一条 assistant 消息
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd backend && python -m pytest tests/test_resume_guard.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/routers/conversations.py backend/tests/test_resume_guard.py
git commit -m "fix(resume): 已取消/失败的对话不自动恢复，需用户显式重试"
```

---

## Task 5: 后端防重复执行 — run_phase 过滤

**Files:**
- Modify: `backend/db/agents.py:701` (create_pending_agent_run 加 run_phase 参数)
- Modify: `backend/agent/orchestrator.py:3659-3666` (_stream_route 恢复模式过滤)
- Test: `backend/tests/test_run_phase.py` (新建)

**问题根因**: 对话 #84 中 risk_assessor 跑 9 次，因为 cross_review 和仲裁阶段用同一 agent_key 创建 agent_run，恢复模式无法区分"首轮已完成"和"交叉审阅重跑"。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_run_phase.py
"""测试 run_phase 字段过滤：恢复模式只排除 primary 阶段已完成的专家。"""
import pytest
from db.agents import create_pending_agent_run, get_completed_agents_for_message, update_agent_run_status
from db.conversations import create_conversation, create_message


def test_run_phase_filter():
    """primary 阶段已完成的专家应被排除，cross_review 阶段的不影响。"""
    conv_id = create_conversation("test run_phase")
    msg_id = create_message(conv_id, "user", "test")
    try:
        # 创建 primary 阶段的 risk_assessor run（已完成）
        run1 = create_pending_agent_run(conv_id, msg_id, "risk_assessor", "风控专家", trace_id="t1")
        update_agent_run_status(run1, "completed", run_phase="primary")

        # 创建 cross_review 阶段的 risk_assessor run（不应影响 primary 过滤）
        run2 = create_pending_agent_run(conv_id, msg_id, "risk_assessor", "风控专家", trace_id="t1")
        update_agent_run_status(run2, "completed", run_phase="cross_review")

        # 查询 primary 阶段已完成的专家
        completed_primary = get_completed_agents_for_message(msg_id, run_phase="primary")
        assert "risk_assessor" in completed_primary, "primary 阶段已完成的专家应被返回"

        # 查询所有阶段已完成的专家（向后兼容）
        completed_all = get_completed_agents_for_message(msg_id)
        assert "risk_assessor" in completed_all, "不传 run_phase 时应返回所有"
    finally:
        from db.conversations import delete_conversation
        delete_conversation(conv_id)
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python -m pytest tests/test_run_phase.py -v`
Expected: FAIL with "unexpected keyword argument 'run_phase'"

- [ ] **Step 3: 修改 create_pending_agent_run 支持 run_phase**

在 `backend/db/agents.py` 的 `create_pending_agent_run` 函数（约 701 行）修改签名和 SQL：

```python
# 修改前（约 701 行）：
def create_pending_agent_run(conversation_id: int, message_id: int, agent_key: str,
                              agent_name: str, query: str = "", trace_id: str = "") -> int:
    """创建 pending 状态的 agent 执行记录，返回 run_id。"""
    conn = _get_conn()
    cur = conn.execute("""
        INSERT INTO agent_runs (conversation_id, message_id, agent_key, agent_name,
                                query, trace_id, status)
        VALUES (?, ?, ?, ?, ?, ?, 'pending')
    """, (conversation_id, message_id, agent_key, agent_name, query, trace_id))
    # ...

# 修改后：
def create_pending_agent_run(conversation_id: int, message_id: int, agent_key: str,
                              agent_name: str, query: str = "", trace_id: str = "",
                              run_phase: str = "primary") -> int:
    """创建 pending 状态的 agent 执行记录，返回 run_id。run_phase: primary/cross_review/arbitration。"""
    conn = _get_conn()
    cur = conn.execute("""
        INSERT INTO agent_runs (conversation_id, message_id, agent_key, agent_name,
                                query, trace_id, status, run_phase)
        VALUES (?, ?, ?, ?, ?, ?, 'pending', ?)
    """, (conversation_id, message_id, agent_key, agent_name, query, trace_id, run_phase))
    run_id = cur.lastrowid
    conn.commit()
    conn.close()
    return run_id
```

- [ ] **Step 4: 修改 update_agent_run_status 支持 run_phase**

在 `backend/db/agents.py` 的 `update_agent_run_status` 函数（约 716 行）增加 run_phase 可选参数：

```python
# 修改前（约 716 行）：
def update_agent_run_status(run_id: int, status: str, result: str = None,
                             duration_ms: int = None, error_message: str = None):
    # ...

# 修改后：
def update_agent_run_status(run_id: int, status: str, result: str = None,
                             duration_ms: int = None, error_message: str = None,
                             run_phase: str = None):
    """更新 agent 执行记录状态。status: running / completed / failed / cancelled。"""
    conn = _get_conn()
    updates = ["status = ?"]
    params = [status]

    if result is not None:
        updates.append("result = ?")
        params.append(result)
    if duration_ms is not None:
        updates.append("duration_ms = ?")
        params.append(duration_ms)
    if run_phase is not None:
        updates.append("run_phase = ?")
        params.append(run_phase)
    if status == "completed":
        # ... 保持原有逻辑
```

- [ ] **Step 5: 修改 get_completed_agents_for_message 支持 run_phase 过滤**

在 `backend/db/agents.py` 找到 `get_completed_agents_for_message` 函数，修改为：

```python
def get_completed_agents_for_message(message_id: int, run_phase: str = None) -> list[dict]:
    """获取某消息已完成的专家列表。run_phase 为 None 时返回所有阶段。"""
    conn = _get_conn()
    if run_phase:
        rows = conn.execute("""
            SELECT * FROM agent_runs
            WHERE message_id = ? AND status = 'completed' AND run_phase = ?
            ORDER BY id
        """, (message_id, run_phase)).fetchall()
    else:
        rows = conn.execute("""
            SELECT * FROM agent_runs
            WHERE message_id = ? AND status = 'completed'
            ORDER BY id
        """, (message_id,)).fetchall()
    conn.close()
    return [dict(r) for r in rows]
```

- [ ] **Step 6: 修改 _stream_route 恢复模式用 run_phase='primary' 过滤**

在 `backend/agent/orchestrator.py` 的 `_stream_precheck` 函数中（约 3570 行之前），找到调用 `get_completed_agents_for_message` 的位置，修改为传 `run_phase='primary'`：

```python
# 修改前（约 _stream_precheck 中）：
completed_runs = get_completed_agents_for_message(message_id)

# 修改后：
completed_runs = get_completed_agents_for_message(message_id, run_phase='primary')
```

同样在 `backend/routers/conversations.py` 的 `resume_conversation` 函数中（约 247 行）：

```python
# 修改前：
completed_runs = get_completed_agents_for_message(message_id)

# 修改后：
completed_runs = get_completed_agents_for_message(message_id, run_phase='primary')
```

- [ ] **Step 7: 运行测试验证通过**

Run: `cd backend && python -m pytest tests/test_run_phase.py -v`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add backend/db/agents.py backend/agent/orchestrator.py backend/routers/conversations.py backend/tests/test_run_phase.py
git commit -m "feat(anti-dup): run_phase 字段区分执行阶段，恢复模式只排除 primary"
```

---

## Task 6: 持仓上下文注入 — _build_portfolio_context()

**Files:**
- Modify: `backend/agent/orchestrator.py` (新增函数 + 调用)
- Test: `backend/tests/test_portfolio_context.py` (新建)

**目标**: 让每个专家看到用户的实际持仓（基金名、占比、盈亏率、成本），避免"估值低就加仓但不看已亏损"的 bad case。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_portfolio_context.py
"""测试持仓上下文构建。"""
import pytest
from agent.orchestrator import _build_portfolio_context


def test_portfolio_context_contains_holdings():
    """持仓上下文应包含基金名、占比、盈亏率。"""
    ctx = _build_portfolio_context()
    assert isinstance(ctx, str)
    # 如果有持仓，应包含"当前持仓"标题
    if ctx:
        assert "当前持仓" in ctx or "无持仓" in ctx


def test_portfolio_context_format():
    """持仓上下文格式应包含关键字段。"""
    ctx = _build_portfolio_context()
    if "无持仓" in ctx:
        return  # 无持仓时跳过格式检查
    # 应包含占比或盈亏相关字段
    assert "占比" in ctx or "盈亏" in ctx or "成本" in ctx
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python -m pytest tests/test_portfolio_context.py -v`
Expected: FAIL with "cannot import name '_build_portfolio_context'"

- [ ] **Step 3: 实现 _build_portfolio_context()**

在 `backend/agent/orchestrator.py` 的 `_build_history_summary` 函数（约 460 行）之后添加：

```python
def _build_portfolio_context() -> str:
    """构建用户持仓上下文，注入每个专家的 system_prompt。

    包含：基金名、持仓占比、盈亏率、成本价、当前价。
    让专家在推荐加减仓时能看到用户实际持仓，避免"估值低就加仓但不看已亏损"。
    """
    try:
        from db.portfolio import get_portfolio_summary
        summary = get_portfolio_summary()
        if not summary or not summary.get("holdings"):
            return "【当前持仓】无持仓数据"

        lines = ["【当前持仓】"]
        total_cost = summary.get("total_cost", 0)
        total_value = summary.get("total_value", 0)
        total_pnl = total_value - total_cost
        total_pnl_pct = (total_pnl / total_cost * 100) if total_cost > 0 else 0
        lines.append(f"总成本: ¥{total_cost:,.0f} | 总市值: ¥{total_value:,.0f} | 总盈亏: ¥{total_pnl:,.0f} ({total_pnl_pct:+.1f}%)")
        lines.append("")

        for h in summary["holdings"][:10]:  # 最多 10 条
            name = h.get("fund_name", h.get("fund_code", ""))
            code = h.get("fund_code", "")
            shares = h.get("shares", 0)
            cost = h.get("cost_price", 0)
            current = h.get("current_price", 0)
            value = h.get("market_value", shares * current)
            pnl = (current - cost) * shares if cost > 0 else 0
            pnl_pct = ((current - cost) / cost * 100) if cost > 0 else 0
            weight = (value / total_value * 100) if total_value > 0 else 0
            lines.append(
                f"- {name}({code}): 持仓{shares:,.0f}份 | 成本{cost:.4f} | 现价{current:.4f} | "
                f"市值¥{value:,.0f} | 占比{weight:.1f}% | 盈亏¥{pnl:,.0f}({pnl_pct:+.1f}%)"
            )
        return "\n".join(lines)
    except Exception as e:
        logger.warning(f"构建持仓上下文失败: {e}")
        return ""
```

- [ ] **Step 4: 在专家调用时注入持仓上下文**

在 `backend/agent/orchestrator.py` 中找到构建专家 system_prompt 的位置（搜索 `prebuilt_context` 或 `system_prompt`），在专家执行前注入持仓上下文。找到 `_stream_route` 返回后的 specialists 执行阶段，在 prebuilt_context 构建处追加：

```python
# 在构建 prebuilt_context 的位置追加持仓上下文（搜索 prebuilt_context 赋值处）：
portfolio_ctx = _build_portfolio_context()
if portfolio_ctx:
    prebuilt_context = (prebuilt_context or "") + "\n\n" + portfolio_ctx
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd backend && python -m pytest tests/test_portfolio_context.py -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/agent/orchestrator.py backend/tests/test_portfolio_context.py
git commit -m "feat(context): 注入持仓上下文到专家 prompt，避免不看盈亏就建议加仓"
```

---

## Task 7: 4% 定投法规则注入 — _build_dca_rules()

**Files:**
- Modify: `backend/agent/orchestrator.py` (新增函数 + 调用)
- Test: `backend/tests/test_dca_rules.py` (新建)

**目标**: 让估值分析师和资产配置师遵守 4% 定投法规则（基础档 ¥500，每跌 4% 加一档，最多 3 档），避免加仓金额随意。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_dca_rules.py
"""测试 4% 定投法规则注入。"""
import pytest
from agent.orchestrator import _build_dca_rules


def test_dca_rules_contains_key_rules():
    """定投规则应包含基础金额、跌幅档位、最大档数。"""
    rules = _build_dca_rules()
    assert isinstance(rules, str)
    assert "500" in rules  # 基础定投金额
    assert "4%" in rules or "4%" in rules  # 跌幅档位
    assert "3" in rules  # 最大档数


def test_dca_rules_contains_reduce_limits():
    """规则应包含减仓上限约束。"""
    rules = _build_dca_rules()
    assert "20%" in rules  # 单基金减仓上限
    assert "10%" in rules  # 总减仓上限
    assert "50,000" in rules or "50000" in rules  # 单次减仓金额上限
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python -m pytest tests/test_dca_rules.py -v`
Expected: FAIL with "cannot import name '_build_dca_rules'"

- [ ] **Step 3: 实现 _build_dca_rules()**

在 `backend/agent/orchestrator.py` 的 `_build_portfolio_context` 函数之后添加：

```python
def _build_dca_rules() -> str:
    """构建 4% 定投法规则和减仓约束，注入估值分析师和资产配置师的 prompt。

    规则来源：daily_advice.base_dca_amount、daily_advice.dca_drop_step_pct、daily_advice.max_dca_steps
    """
    try:
        from db.config import get_config
        base_amount = get_config("daily_advice.base_dca_amount", "500")
        drop_step = get_config("daily_advice.dca_drop_step_pct", "4")
        max_steps = get_config("daily_advice.max_dca_steps", "3")

        return f"""【加减仓规则约束（必须遵守）】

## 4% 定投法（加仓规则）
- 基础定投金额：¥{base_amount}/档
- 跌幅档位：每跌 {drop_step}% 加一档
- 最大档数：{max_steps} 档（即最大加仓 ¥{int(base_amount) * int(max_steps)}）
- 加仓前提：估值百分位 ≤ 35% 且不在补仓冷静期（10 天内）
- 加仓金额计算：¥{base_amount} × 档数（跌幅/{drop_step}%，向下取整，上限 {max_steps} 档）

## 减仓约束（必须遵守）
- 单次减仓不超过该基金持仓的 20%
- 单次建议总减仓金额不超过总资产的 10%
- 禁止同时减仓 2 个以上基金
- 禁止一次性减仓超过 ¥50,000
- 减仓前提：估值百分位 ≥ 80% 且有明确止盈信号

## 禁止事项
- 禁止在未查看用户持仓盈亏的情况下建议加仓
- 禁止建议加仓已超持仓上限（25%）的基金
- 禁止建议减仓亏损基金（除非风险止损场景）"""
    except Exception as e:
        logger.warning(f"构建定投规则失败: {e}")
        return ""
```

- [ ] **Step 4: 在 prebuilt_context 中追加定投规则**

在 `backend/agent/orchestrator.py` 中，找到 Task 6 注入 `portfolio_ctx` 的同一位置，追加定投规则：

```python
# 在 portfolio_ctx 注入之后追加：
dca_rules = _build_dca_rules()
if dca_rules:
    prebuilt_context = (prebuilt_context or "") + "\n\n" + dca_rules
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd backend && python -m pytest tests/test_dca_rules.py -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/agent/orchestrator.py backend/tests/test_dca_rules.py
git commit -m "feat(context): 注入 4% 定投法规则和减仓约束到专家 prompt"
```

---

## Task 8: 前端失败/取消状态显式化

**Files:**
- Modify: `frontend/src/components/ChatView.vue` (消息状态显示 + 恢复守卫)
- Test: 手动测试

**问题根因**: 对话 #85 失败后，前端继续显示"⏳ 分析进行中..."，不处理 failed/cancelled 状态。

- [ ] **Step 1: 找到前端消息渲染和恢复逻辑**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer && grep -n "execution_status\|分析进行中\|resumeConversation\|loadConversation\|onMounted" frontend/src/components/ChatView.vue | head -20`

找到以下关键位置：
- 消息渲染（显示"分析进行中"的位置）
- onMounted 或 loadConversation（恢复对话的位置）

- [ ] **Step 2: 修改消息状态显示**

在 `frontend/src/components/ChatView.vue` 中，找到渲染 assistant 消息的位置，添加状态判断：

```javascript
// 在消息渲染相关位置添加（具体行号需根据 Step 1 结果定位）：
function getMessageDisplayStatus(message) {
  let meta = message.metadata
  if (typeof meta === 'string') {
    try { meta = JSON.parse(meta) } catch { meta = {} }
  }
  if (!meta) return null
  const status = meta.execution_status
  if (status === 'failed') {
    return { text: '❌ 分析失败，点击重试', action: 'retry', class: 'status-failed' }
  }
  if (status === 'cancelled') {
    return { text: '🚫 已取消', action: null, class: 'status-cancelled' }
  }
  if (status === 'streaming' || status === 'queued' || status === 'pending') {
    return { text: '⏳ 分析进行中...', action: 'cancel', class: 'status-streaming' }
  }
  return null  // 正常完成
}
```

- [ ] **Step 3: 修改恢复逻辑加守卫**

在 `frontend/src/components/ChatView.vue` 的 onMounted 或 loadConversation 中，添加取消/失败守卫：

```javascript
// 在自动调用 resume 的位置加守卫（具体行号需根据 Step 1 结果定位）：
async function resumeConversation(convId) {
  try {
    const resp = await api.resumeConversation(convId)
    // 正常恢复 SSE 流
    startSSEStream(resp)
  } catch (err) {
    if (err.response && err.response.status === 409) {
      // 409 表示对话已取消或失败，不自动恢复
      const detail = err.response.data?.detail || ''
      if (detail.includes('已取消')) {
        // 显示取消状态，不自动重试
        console.log('对话已取消，不自动恢复')
      } else if (detail.includes('失败')) {
        // 显示失败状态 + 重试按钮
        console.log('对话上次失败，显示重试按钮')
      }
    } else {
      console.error('恢复对话失败:', err)
    }
  }
}
```

- [ ] **Step 4: 添加重试按钮逻辑**

在消息渲染模板中，为 failed 状态的消息添加重试按钮：

```vue
<!-- 在 assistant 消息渲染模板中添加（具体位置根据模板结构） -->
<div v-if="getMessageDisplayStatus(message)?.action === 'retry'" class="retry-button" @click="retryMessage(message)">
  {{ getMessageDisplayStatus(message).text }}
</div>
<div v-else-if="getMessageDisplayStatus(message)" :class="getMessageDisplayStatus(message).class">
  {{ getMessageDisplayStatus(message).text }}
</div>
```

```javascript
async function retryMessage(message) {
  // 调用重试 API
  try {
    const resp = await api.retryMessage(message.id)
    // 清除 cancel_requested 标记
    await api.clearCancelFlag(message.conversation_id)
    // 重新开始 SSE 流
    startSSEStream(resp)
  } catch (err) {
    console.error('重试失败:', err)
  }
}
```

- [ ] **Step 5: 添加 API 函数**

在 `frontend/src/api/index.js` 中添加：

```javascript
// 重试消息
export const retryMessage = (messageId) => api.post(`/api/conversations/messages/${messageId}/retry`)

// 清除取消标记
export const clearCancelFlag = (convId) => api.post(`/api/conversations/${convId}/clear-cancel`)
```

- [ ] **Step 6: 添加后端 clear-cancel 接口**

在 `backend/routers/conversations.py` 中添加：

```python
@router.post("/api/conversations/{conv_id}/clear-cancel")
async def clear_cancel_flag(conv_id: int):
    """清除取消标记（用户点击重试时调用）。"""
    from db.conversations import clear_conversation_cancel_flag
    clear_conversation_cancel_flag(conv_id)
    return {"ok": True}
```

- [ ] **Step 7: 手动测试**

Run: `cd backend && uvicorn app:app --reload --port 8000`
Test:
1. 发起一个对话，在分析过程中点击取消
2. 刷新页面 → 应看到"🚫 已取消"，不自动重试
3. 发起一个对话，断网模拟失败
4. 刷新页面 → 应看到"❌ 分析失败，点击重试"
5. 点击重试按钮 → 应重新开始分析

- [ ] **Step 8: 提交**

```bash
git add frontend/src/components/ChatView.vue frontend/src/api/index.js backend/routers/conversations.py
git commit -m "feat(frontend): 失败/取消状态显式化，重试需用户主动点击"
```

---

## 验收清单

实施完成后，执行以下验收测试：

- [ ] **V1: 取消后刷新不自动重试**
  - 发起对话 → 分析中点击取消 → 刷新页面
  - 预期：显示"🚫 已取消"，不自动调用 resume

- [ ] **V2: 失败状态显式显示**
  - 发起对话 → 模拟失败（断网或 kill 后台）
  - 预期：显示"❌ 分析失败，点击重试"，不自动调用 resume

- [ ] **V3: 重试按钮可用**
  - 点击"❌ 分析失败，点击重试"
  - 预期：清除 cancel_requested，重新开始 SSE 流

- [ ] **V4: 防重复执行**
  - 发起 complex 对话，完成后查询 `SELECT agent_key, run_phase, COUNT(*) FROM agent_runs WHERE conversation_id=? GROUP BY agent_key, run_phase`
  - 预期：每个专家 primary 阶段只有 1 条记录

- [ ] **V5: 持仓上下文注入**
  - 发起对话问"加仓 XX 基金"
  - 预期：回答中包含"占比 X%"、"盈亏 X%"等持仓信息

- [ ] **V6: 4% 定投法约束**
  - 发起对话问"加仓建议"
  - 预期：加仓金额 = ¥500 × 档数，不超过 3 档

- [ ] **V7: 减仓上限约束**
  - 发起对话问"减仓建议"
  - 预期：单次减仓 ≤ 持仓 20%，总额 ≤ ¥50,000

- [ ] **V8: 配置值已同步**
  - Run: `sqlite3 backend/data/valuations.db "SELECT key, value FROM system_config WHERE key IN ('agent.reuse_recent_conclusions','cost_routing.arbitrator_model')"`
  - 预期：reuse=true, arbitrator=deepseek-v4-pro

- [ ] **V9: 单元测试全部通过**
  - Run: `cd backend && python -m pytest tests/test_schema_migration.py tests/test_cancel_persist.py tests/test_resume_guard.py tests/test_run_phase.py tests/test_portfolio_context.py tests/test_dca_rules.py -v`
  - 预期：全部 PASS

---

## 风险与回滚

| Task | 风险 | 回滚方式 |
|------|------|---------|
| Task 1 (DB schema) | 低（_add_column_if_not_exists 向后兼容） | 字段默认值，旧代码不读不影响 |
| Task 2 (DB config) | 低（配置值更新） | 重新运行 sync_config.py 改回旧值 |
| Task 3 (cancel persist) | 低（新增字段+函数） | cancel 接口不调用 mark_conversation_cancelled |
| Task 4 (resume guard) | 中（改变 resume 行为） | 移除 409 守卫，恢复自动 resume |
| Task 5 (run_phase) | 中（改 agent_runs 表+查询） | run_phase 默认 'primary'，旧查询兼容 |
| Task 6 (portfolio ctx) | 低（prompt 追加） | 移除 _build_portfolio_context 调用 |
| Task 7 (DCA rules) | 低（prompt 追加） | 移除 _build_dca_rules 调用 |
| Task 8 (frontend) | 中（改前端交互） | 回退 ChatView.vue 改动 |
