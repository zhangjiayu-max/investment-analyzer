# 持仓管理优化 P0 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现持仓管理 P0 优化：加减仓智能化（软提示）+ 交易流程优化（自动确认/撤销入口/筛选）

**Architecture:** 后端新增3个 API（dca-suggestion/sell-preview/auto-confirm）+ 改造1个 API（list_transactions 日期筛选）；前端改造加仓/减仓/确认弹窗 + 交易记录列表（撤销入口/筛选/批量确认）

**Tech Stack:** Python 3.11+ / FastAPI / SQLite / Vue 3 (Composition API + `<script setup>`) / axios

**设计稿:** `doc/plans/2026-07-07-持仓管理优化-design.md`

---

## 文件结构

### 后端
- `backend/db/portfolio.py` — 新增 `get_dca_suggestion()`、`preview_sell()`、`get_nav_by_date()`；改造 `list_transactions()` 增加日期筛选
- `backend/routers/portfolio.py` — 新增 3 个路由：`dca-suggestion`、`sell-preview`、`auto-confirm`
- `backend/models/portfolio.py` — 新增 `SellPreviewRequest` 请求模型

### 前端
- `frontend/src/api/index.js` — 新增 `getDcaSuggestion()`、`previewSell()`、`autoConfirmTransaction()`、`listTransactionsWithFilter()`
- `frontend/src/components/PortfolioManagement.vue` — 改造加仓/减仓/确认弹窗 + 交易记录列表

### 测试
- `backend/tests/test_dca_suggestion.py`
- `backend/tests/test_sell_preview.py`
- `backend/tests/test_auto_confirm.py`
- `backend/tests/test_transaction_filter.py`

---

## Task 1: 后端 — 加仓建议 API

**Files:**
- Modify: `backend/db/portfolio.py`（新增 `get_dca_suggestion()` 函数）
- Modify: `backend/routers/portfolio.py`（新增路由）
- Test: `backend/tests/test_dca_suggestion.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_dca_suggestion.py`：

```python
# backend/tests/test_dca_suggestion.py
"""测试加仓建议 API。"""
import pytest
from db.conversations import create_conversation, delete_conversation
from db.portfolio import create_holding, delete_holding, get_dca_suggestion


def _setup_holding(profit_rate=None, shares=1000, cost_price=1.0, current_price=None):
    """创建测试持仓。profit_rate<0 亏损，>0 盈利。"""
    if current_price is None:
        # 通过调整 current_price 控制盈亏率
        current_price = cost_price * (1 + (profit_rate or 0))
    holding_id = create_holding(
        fund_code="110022", fund_name="易方达消费",
        shares=shares, cost_price=cost_price, current_price=current_price,
    )
    return holding_id


def test_dca_suggestion_profit_advice_watch():
    """盈利状态建议观望。"""
    holding_id = _setup_holding(profit_rate=0.05)  # 盈利5%
    try:
        result = get_dca_suggestion(holding_id)
        assert result["advice"] == "watch"
        assert result["suggestion"]["recommended_amount"] == 0
        assert "盈利" in result["suggestion"]["rule"]
    finally:
        delete_holding(holding_id)


def test_dca_suggestion_small_loss_tier1():
    """亏损0~4%建议第1档¥500。"""
    holding_id = _setup_holding(profit_rate=-0.02)  # 亏损2%
    try:
        result = get_dca_suggestion(holding_id)
        assert result["advice"] == "continue"
        assert result["suggestion"]["recommended_amount"] == 500
        assert result["suggestion"]["tier"] == 1
    finally:
        delete_holding(holding_id)


def test_dca_suggestion_medium_loss_tier2():
    """亏损4~8%建议第2档¥1000。"""
    holding_id = _setup_holding(profit_rate=-0.06)  # 亏损6%
    try:
        result = get_dca_suggestion(holding_id)
        assert result["advice"] == "continue"
        assert result["suggestion"]["recommended_amount"] == 1000
        assert result["suggestion"]["tier"] == 2
    finally:
        delete_holding(holding_id)


def test_dca_suggestion_large_loss_tier3():
    """亏损8~12%建议第3档¥1500。"""
    holding_id = _setup_holding(profit_rate=-0.10)  # 亏损10%
    try:
        result = get_dca_suggestion(holding_id)
        assert result["advice"] == "continue"
        assert result["suggestion"]["recommended_amount"] == 1500
        assert result["suggestion"]["tier"] == 3
    finally:
        delete_holding(holding_id)


def test_dca_suggestion_huge_loss_capped():
    """亏损>12%封顶第3档+提示。"""
    holding_id = _setup_holding(profit_rate=-0.15)  # 亏损15%
    try:
        result = get_dca_suggestion(holding_id)
        assert result["suggestion"]["recommended_amount"] == 1500
        assert result["suggestion"]["tier"] == 3
        assert "超过定投法覆盖范围" in result["suggestion"]["rule"]
    finally:
        delete_holding(holding_id)
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python3.13 -m pytest tests/test_dca_suggestion.py -v`
Expected: FAIL with "ImportError: cannot import name 'get_dca_suggestion'"

- [ ] **Step 3: 实现 `get_dca_suggestion()`**

在 `backend/db/portfolio.py` 的 `get_holding_by_fund` 函数之后（约120行后）新增：

```python
def get_dca_suggestion(holding_id: int) -> dict:
    """根据4%定投法返回加仓建议。

    规则：基础¥500/档，每跌4%加一档，最多3档。
    - 盈利：建议观望（recommended_amount=0, advice=watch）
    - 亏损 0~4%：第1档 ¥500
    - 亏损 4~8%：第2档 ¥1000
    - 亏损 8~12%：第3档 ¥1500
    - 亏损 >12%：封顶¥1500 + 提示超过覆盖范围
    """
    holding = get_holding(holding_id)
    if not holding:
        return {"error": "持仓不存在"}

    profit_rate = holding.get("profit_rate")
    fund_code = holding["fund_code"]
    fund_name = holding.get("fund_name", fund_code)

    # 查询近30天加仓次数（用户手动买入，排除系统交易）
    conn = _get_conn()
    try:
        from datetime import datetime, timedelta
        cutoff = (datetime.now() - timedelta(days=30)).strftime("%Y-%m-%d")
        recent_count = conn.execute("""
            SELECT COUNT(*) as cnt FROM portfolio_transactions
            WHERE holding_id = ? AND transaction_type = 'buy'
              AND (is_system IS NULL OR is_system = 0)
              AND transaction_date >= ?
        """, (holding_id, cutoff)).fetchone()["cnt"]
        # 今日是否已加仓
        today = datetime.now().strftime("%Y-%m-%d")
        today_count = conn.execute("""
            SELECT COUNT(*) as cnt FROM portfolio_transactions
            WHERE holding_id = ? AND transaction_type = 'buy'
              AND (is_system IS NULL OR is_system = 0)
              AND transaction_date = ?
        """, (holding_id, today)).fetchone()["cnt"]
    finally:
        conn.close()

    # 计算建议档位
    BASE_AMOUNT = 500
    TIER_STEP = 500
    MAX_TIERS = 3

    if profit_rate is None:
        advice = "watch"
        recommended = 0
        tier = 0
        rule = "无法计算盈亏率，建议观望"
    elif profit_rate > 0:
        advice = "watch"
        recommended = 0
        tier = 0
        rule = f"当前盈利 {profit_rate*100:.1f}%，建议观望"
    else:
        loss_pct = -profit_rate * 100  # 转为正数百分比
        tier = min(int(loss_pct / 4) + 1, MAX_TIERS)
        recommended = BASE_AMOUNT + (tier - 1) * TIER_STEP
        if tier >= MAX_TIERS and loss_pct > 12:
            advice = "continue"
            rule = f"基础¥500/档，每跌4%加一档，当前亏损{loss_pct:.1f}%对应第{tier}档（已超过定投法覆盖范围，建议结合基本面分析）"
        else:
            advice = "continue"
            rule = f"基础¥500/档，每跌4%加一档，当前亏损{loss_pct:.1f}%对应第{tier}档"

    # 近期加仓频繁提示
    if recent_count >= 3:
        advice = "pause"
        rule += f"；近30天已加仓{recent_count}次，建议放缓节奏"

    return {
        "fund_code": fund_code,
        "fund_name": fund_name,
        "current_profit_rate": profit_rate,
        "current_shares": holding.get("shares", 0),
        "current_value": holding.get("current_value"),
        "suggestion": {
            "recommended_amount": recommended,
            "tier": tier,
            "max_tiers": MAX_TIERS,
            "rule": rule,
            "already_added_today": today_count > 0,
            "recent_add_count_30d": recent_count,
        },
        "advice": advice,
    }
```

- [ ] **Step 4: 新增路由**

在 `backend/routers/portfolio.py` 的 `refresh_single_price_api` 函数之后新增：

```python
@router.get("/api/portfolio/{holding_id}/dca-suggestion")
async def get_dca_suggestion_api(holding_id: int):
    """获取加仓建议（4%定投法）。"""
    from db.portfolio import get_dca_suggestion
    result = get_dca_suggestion(holding_id)
    if "error" in result:
        raise HTTPException(404, result["error"])
    return result
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd backend && python3.13 -m pytest tests/test_dca_suggestion.py -v`
Expected: 5 passed

- [ ] **Step 6: 提交**

```bash
git add backend/db/portfolio.py backend/routers/portfolio.py backend/tests/test_dca_suggestion.py
git commit -m "feat(portfolio): 加仓建议 API（4%定投法，软提示）"
```

---

## Task 2: 前端 — 加仓弹窗增加建议卡片

**Files:**
- Modify: `frontend/src/api/index.js`（新增 `getDcaSuggestion()`）
- Modify: `frontend/src/components/PortfolioManagement.vue`（改造加仓弹窗）

- [ ] **Step 1: 新增 API 函数**

在 `frontend/src/api/index.js` 的 `refreshPortfolioPrice` 函数之后新增：

```javascript
/** 获取加仓建议（4%定投法） */
export function getDcaSuggestion(holdingId) {
  return api.get(`/portfolio/${holdingId}/dca-suggestion`)
}
```

- [ ] **Step 2: 前端导入并增加状态**

在 `frontend/src/components/PortfolioManagement.vue` 的 `<script setup>` 导入区，将 `getDcaSuggestion` 加入 import。在 `showAddPurchase` 附近（约613行）新增：

```javascript
const dcaSuggestion = ref(null)
const dcaLoading = ref(false)
```

- [ ] **Step 3: 改造 `openAddPurchase` 获取建议**

修改 `openAddPurchase` 函数（约2926行），在 `showAddPurchase.value = true` 之前增加获取建议：

```javascript
function openAddPurchase(h) {
  addPurchaseHolding.value = h
  const now = new Date()
  addPurchaseForm.value = {
    amount: 0,
    transaction_date: now.toISOString().slice(0, 10),
    transaction_time: now.toTimeString().slice(0, 5),
    notes: '',
  }
  dcaSuggestion.value = null
  dcaLoading.value = true
  showAddPurchase.value = true
  // 获取加仓建议（软提示，失败不阻塞）
  getDcaSuggestion(h.id).then(({ data }) => {
    dcaSuggestion.value = data
  }).catch(() => {}).finally(() => {
    dcaLoading.value = false
  })
}

function applyDcaSuggestion() {
  if (dcaSuggestion.value?.suggestion?.recommended_amount > 0) {
    addPurchaseForm.value.amount = dcaSuggestion.value.suggestion.recommended_amount
  }
}
```

- [ ] **Step 4: 弹窗模板增加建议卡片**

在加仓弹窗的 `<p class="modal-subtitle">` 之后、`<form>` 之前（约5283行）插入建议卡片：

```html
<!-- 加仓建议（软提示） -->
<div v-if="dcaLoading" class="dca-suggestion-card loading">
  <span>正在计算加仓建议...</span>
</div>
<div v-else-if="dcaSuggestion" class="dca-suggestion-card" :class="dcaSuggestion.advice">
  <div class="dca-header">
    <span class="dca-label">加仓建议</span>
    <span v-if="dcaSuggestion.current_profit_rate !== null" class="dca-profit" :class="{ loss: dcaSuggestion.current_profit_rate < 0 }">
      当前盈亏 {{ (dcaSuggestion.current_profit_rate * 100).toFixed(1) }}%
    </span>
  </div>
  <p class="dca-rule">{{ dcaSuggestion.suggestion.rule }}</p>
  <div v-if="dcaSuggestion.suggestion.recommended_amount > 0" class="dca-action">
    <span class="dca-amount">建议金额 ¥{{ dcaSuggestion.suggestion.recommended_amount }}</span>
    <button type="button" class="btn-sm btn-primary" @click="applyDcaSuggestion">填入建议</button>
  </div>
  <p v-if="dcaSuggestion.suggestion.already_added_today" class="dca-warning">今日已加仓，请谨慎操作</p>
</div>
```

- [ ] **Step 5: 增加 CSS 样式**

在 `<style scoped>` 区域新增（查找已有的 `.add-purchase-preview` 附近）：

```css
.dca-suggestion-card {
  margin: 8px 0 16px;
  padding: 12px;
  border-radius: 8px;
  background: #f0f9ff;
  border: 1px solid #bae6fd;
}
.dca-suggestion-card.loading { color: #888; }
.dca-suggestion-card.watch { background: #fefce8; border-color: #fef08a; }
.dca-suggestion-card.pause { background: #fef2f2; border-color: #fecaca; }
.dca-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 6px; }
.dca-label { font-weight: 600; font-size: 13px; }
.dca-profit { font-size: 13px; color: #16a34a; }
.dca-profit.loss { color: #dc2626; }
.dca-rule { font-size: 12px; color: #666; margin: 4px 0; line-height: 1.5; }
.dca-action { display: flex; justify-content: space-between; align-items: center; margin-top: 8px; }
.dca-amount { font-weight: 600; color: #2563eb; }
.dca-warning { font-size: 12px; color: #dc2626; margin-top: 6px; }
```

- [ ] **Step 6: 验证前端构建**

Run: `cd frontend && npm run build`
Expected: build 成功无报错

- [ ] **Step 7: 提交**

```bash
git add frontend/src/api/index.js frontend/src/components/PortfolioManagement.vue
git commit -m "feat(portfolio-ui): 加仓弹窗增加4%定投法建议卡片（软提示）"
```

---

## Task 3: 后端 — 减仓预览 API

**Files:**
- Modify: `backend/models/portfolio.py`（新增 `SellPreviewRequest`）
- Modify: `backend/db/portfolio.py`（新增 `preview_sell()`）
- Modify: `backend/routers/portfolio.py`（新增路由）
- Test: `backend/tests/test_sell_preview.py`

- [ ] **Step 1: 新增请求模型**

在 `backend/models/portfolio.py` 的 `ConfirmTransactionRequest` 之后新增：

```python
class SellPreviewRequest(BaseModel):
    shares: float  # 拟卖出份额
```

- [ ] **Step 2: 写失败测试**

创建 `backend/tests/test_sell_preview.py`：

```python
# backend/tests/test_sell_preview.py
"""测试减仓预览 API。"""
import pytest
from db.portfolio import create_holding, delete_holding, preview_sell


def test_sell_preview_profit_calculation():
    """减仓预览正确计算盈亏。"""
    # 成本1.0，当前1.5，持有1000份，卖出500份
    holding_id = create_holding(
        fund_code="110022", fund_name="易方达消费",
        shares=1000, cost_price=1.0, current_price=1.5,
    )
    try:
        result = preview_sell(holding_id, 500)
        assert result["shares_to_sell"] == 500
        assert result["current_price"] == 1.5
        assert result["expected_proceeds"] == 750.0  # 500 * 1.5
        assert result["cost_basis"] == 1.0
        assert result["expected_profit_loss"] == 250.0  # (1.5-1.0)*500
        assert abs(result["expected_profit_rate"] - 0.5) < 0.01  # 50%
        assert result["remaining_shares"] == 500
    finally:
        delete_holding(holding_id)


def test_sell_preview_loss_warning():
    """亏损状态下减仓触发亏损提示。"""
    holding_id = create_holding(
        fund_code="110023", fund_name="亏损基金",
        shares=1000, cost_price=2.0, current_price=1.5,
    )
    try:
        result = preview_sell(holding_id, 500)
        warning_types = [w["type"] for w in result["warnings"]]
        assert "profit_warning" in warning_types
        profit_warning = next(w for w in result["warnings"] if w["type"] == "profit_warning")
        assert "亏损" in profit_warning["message"]
    finally:
        delete_holding(holding_id)


def test_sell_preview_large_amount_warning():
    """单次减仓金额>50000触发分批提示。"""
    # 成本1.0，当前60，持有10000份，卖出1000份 = 60000元
    holding_id = create_holding(
        fund_code="110024", fund_name="高价基金",
        shares=10000, cost_price=1.0, current_price=60.0,
    )
    try:
        result = preview_sell(holding_id, 1000)
        assert result["expected_proceeds"] == 60000.0
        warning_types = [w["type"] for w in result["warnings"]]
        assert "single_amount" in warning_types
    finally:
        delete_holding(holding_id)


def test_sell_preview_oversell_rejected():
    """卖出份额超过持有份额返回错误。"""
    holding_id = create_holding(
        fund_code="110025", fund_name="测试基金",
        shares=100, cost_price=1.0, current_price=1.0,
    )
    try:
        result = preview_sell(holding_id, 200)
        assert "error" in result
    finally:
        delete_holding(holding_id)
```

- [ ] **Step 3: 运行测试验证失败**

Run: `cd backend && python3.13 -m pytest tests/test_sell_preview.py -v`
Expected: FAIL with "ImportError: cannot import name 'preview_sell'"

- [ ] **Step 4: 实现 `preview_sell()`**

在 `backend/db/portfolio.py` 的 `get_dca_suggestion` 函数之后新增：

```python
def preview_sell(holding_id: int, shares_to_sell: float) -> dict:
    """减仓预览：计算预计盈亏和约束警告（软提示，不拦截）。

    约束检测：
    - profit_warning: 亏损状态下减仓
    - single_amount: 单次减仓 >¥50,000
    - total_reduction: 本次减仓 >总资产10%
    - concentration: 减仓后该基金占比仍 >20%
    """
    holding = get_holding(holding_id)
    if not holding:
        return {"error": "持仓不存在"}

    current_shares = holding.get("shares", 0) or 0
    if shares_to_sell > current_shares:
        return {"error": f"卖出份额 {shares_to_sell} 超过持有份额 {current_shares}"}

    current_price = holding.get("current_price") or 0
    cost_price = holding.get("cost_price") or 0
    fund_code = holding["fund_code"]
    fund_name = holding.get("fund_name", fund_code)

    expected_proceeds = shares_to_sell * current_price
    cost_basis = cost_price
    expected_profit_loss = (current_price - cost_price) * shares_to_sell
    expected_profit_rate = (expected_profit_loss / (cost_price * shares_to_sell)) if (cost_price > 0 and shares_to_sell > 0) else 0
    remaining_shares = current_shares - shares_to_sell
    remaining_value = remaining_shares * current_price

    # 查询总资产
    summary = get_portfolio_summary()
    total_assets = summary.get("total_assets", 0) or 0
    total_value_before = summary.get("total_value", 0) or 0
    total_value_after = total_value_before - expected_proceeds
    concentration_after = (remaining_value / total_value_after) if total_value_after > 0 else 0

    warnings = []

    # 亏损提示
    if expected_profit_loss < 0:
        loss_pct = abs(expected_profit_rate) * 100
        warnings.append({
            "type": "profit_warning",
            "level": "info",
            "message": f"当前亏损 {loss_pct:.1f}%，确认是否止损",
        })

    # 单次金额超限
    if expected_proceeds > 50000:
        warnings.append({
            "type": "single_amount",
            "level": "warning",
            "message": f"本次减仓金额 ¥{expected_proceeds:,.0f} 较大（>¥50,000），建议分批减仓",
        })

    # 总减仓占比超10%
    if total_assets > 0 and expected_proceeds / total_assets > 0.10:
        pct = expected_proceeds / total_assets * 100
        warnings.append({
            "type": "total_reduction",
            "level": "warning",
            "message": f"本次减仓占总资产 {pct:.1f}%（>10%），建议分批减仓",
        })

    # 集中度提示
    if concentration_after > 0.20:
        pct = concentration_after * 100
        warnings.append({
            "type": "concentration",
            "level": "warning",
            "message": f"减仓后该基金仍占总资产 {pct:.1f}%（>20%），集中度偏高",
        })

    return {
        "fund_code": fund_code,
        "fund_name": fund_name,
        "shares_to_sell": shares_to_sell,
        "current_price": current_price,
        "expected_proceeds": round(expected_proceeds, 2),
        "cost_basis": cost_basis,
        "expected_profit_loss": round(expected_profit_loss, 2),
        "expected_profit_rate": round(expected_profit_rate, 4),
        "remaining_shares": remaining_shares,
        "remaining_value": round(remaining_value, 2),
        "total_assets_after": round(total_assets - expected_proceeds, 2),
        "concentration_after": round(concentration_after, 4),
        "warnings": warnings,
    }
```

- [ ] **Step 5: 新增路由**

在 `backend/routers/portfolio.py` 的 `get_dca_suggestion_api` 之后新增：

```python
@router.post("/api/portfolio/{holding_id}/sell-preview")
async def preview_sell_api(holding_id: int, req: SellPreviewRequest):
    """减仓预览：预计盈亏和约束警告。"""
    from db.portfolio import preview_sell
    result = preview_sell(holding_id, req.shares)
    if "error" in result:
        raise HTTPException(400, result["error"])
    return result
```

并在文件顶部 import 区添加 `SellPreviewRequest`。

- [ ] **Step 6: 运行测试验证通过**

Run: `cd backend && python3.13 -m pytest tests/test_sell_preview.py -v`
Expected: 4 passed

- [ ] **Step 7: 提交**

```bash
git add backend/models/portfolio.py backend/db/portfolio.py backend/routers/portfolio.py backend/tests/test_sell_preview.py
git commit -m "feat(portfolio): 减仓预览 API（盈亏计算+约束警告软提示）"
```

---

## Task 4: 前端 — 减仓弹窗增加预览区域

**Files:**
- Modify: `frontend/src/api/index.js`（新增 `previewSell()`）
- Modify: `frontend/src/components/PortfolioManagement.vue`（改造减仓弹窗）

- [ ] **Step 1: 新增 API 函数**

在 `frontend/src/api/index.js` 的 `getDcaSuggestion` 之后新增：

```javascript
/** 减仓预览（预计盈亏+约束警告） */
export function previewSell(holdingId, shares) {
  return api.post(`/portfolio/${holdingId}/sell-preview`, { shares })
}
```

- [ ] **Step 2: 前端增加状态和预览逻辑**

在 `PortfolioManagement.vue` 的 `sellForm` 附近（约1019行）新增：

```javascript
const sellPreview = ref(null)
const sellPreviewLoading = ref(false)

// 监听卖出份额变化，防抖获取预览
let sellPreviewTimer = null
function onSellSharesInput() {
  if (sellPreviewTimer) clearTimeout(sellPreviewTimer)
  const shares = sellForm.value.shares
  if (!shares || shares <= 0) {
    sellPreview.value = null
    return
  }
  sellPreviewTimer = setTimeout(async () => {
    sellPreviewLoading.value = true
    try {
      const { data } = await previewSell(sellHolding.value.id, shares)
      sellPreview.value = data
    } catch (e) {
      sellPreview.value = null
    } finally {
      sellPreviewLoading.value = false
    }
  }, 300)
}
```

- [ ] **Step 3: 改造 `openSell` 清空预览**

修改 `openSell` 函数（约2970行），在末尾增加 `sellPreview.value = null`：

```javascript
function openSell(h) {
  sellHolding.value = h
  const now = new Date()
  sellForm.value = {
    shares: 0,
    transaction_date: now.toISOString().slice(0, 10),
    transaction_time: now.toTimeString().slice(0, 5),
    notes: '',
  }
  sellPreview.value = null
  showSell.value = true
}
```

- [ ] **Step 4: 弹窗模板增加预览区域**

修改减仓弹窗的份额输入框（约5327行），增加 `@input="onSellSharesInput"`：

```html
<input v-model.number="sellForm.shares" type="number" step="0.01" class="input-field" placeholder="如 5000" required @input="onSellSharesInput" />
```

在预估可赎回金额区域（约5329行）之后增加预览卡片：

```html
<!-- 减仓预览（软提示） -->
<div v-if="sellPreviewLoading" class="sell-preview-card loading">
  <span>正在计算减仓预览...</span>
</div>
<div v-else-if="sellPreview" class="sell-preview-card">
  <div class="preview-row">
    <span>预计到账</span>
    <strong>¥{{ Number(sellPreview.expected_proceeds).toLocaleString() }}</strong>
  </div>
  <div class="preview-row">
    <span>预计盈亏</span>
    <span :class="{ profit: sellPreview.expected_profit_loss >= 0, loss: sellPreview.expected_profit_loss < 0 }">
      {{ sellPreview.expected_profit_loss >= 0 ? '+' : '' }}¥{{ Number(sellPreview.expected_profit_loss).toLocaleString() }}
      ({{ (sellPreview.expected_profit_rate * 100).toFixed(1) }}%)
    </span>
  </div>
  <div class="preview-row">
    <span>减仓后剩余</span>
    <span>{{ sellPreview.remaining_shares.toLocaleString() }} 份 · ¥{{ Number(sellPreview.remaining_value).toLocaleString() }}</span>
  </div>
  <div v-for="w in sellPreview.warnings" :key="w.type" class="preview-warning" :class="w.level">
    ⚠ {{ w.message }}
  </div>
</div>
```

- [ ] **Step 5: 增加 CSS 样式**

在 `<style scoped>` 区域新增：

```css
.sell-preview-card {
  margin: 8px 0 16px;
  padding: 12px;
  border-radius: 8px;
  background: #f8fafc;
  border: 1px solid #e2e8f0;
}
.sell-preview-card.loading { color: #888; }
.preview-row { display: flex; justify-content: space-between; align-items: center; padding: 4px 0; font-size: 13px; }
.preview-row .profit { color: #16a34a; }
.preview-row .loss { color: #dc2626; }
.preview-warning { margin-top: 8px; padding: 6px 10px; border-radius: 4px; font-size: 12px; }
.preview-warning.warning { background: #fef3c7; color: #92400e; }
.preview-warning.info { background: #dbeafe; color: #1e40af; }
```

- [ ] **Step 6: 验证前端构建**

Run: `cd frontend && npm run build`
Expected: build 成功

- [ ] **Step 7: 提交**

```bash
git add frontend/src/api/index.js frontend/src/components/PortfolioManagement.vue
git commit -m "feat(portfolio-ui): 减仓弹窗增加盈亏预览和约束警告（软提示）"
```

---

## Task 5: 后端 — 自动获取净值确认 API

**Files:**
- Modify: `backend/db/portfolio.py`（新增 `get_nav_by_date()`）
- Modify: `backend/routers/portfolio.py`（新增路由）
- Test: `backend/tests/test_auto_confirm.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_auto_confirm.py`：

```python
# backend/tests/test_auto_confirm.py
"""测试自动获取净值确认交易。"""
import pytest
from db.portfolio import create_holding, delete_holding, create_transaction, delete_transaction, get_nav_by_date


def test_get_nav_by_date_returns_float_or_none():
    """get_nav_by_date 返回净值或None。"""
    # 查询一个存在的基金代码的净值（可能为None，取决于fund_nav_history是否有数据）
    nav = get_nav_by_date("110022", "2026-01-01")
    assert nav is None or isinstance(nav, (int, float))


def test_auto_confirm_api_endpoint():
    """自动确认API端点存在且返回正确结构。"""
    from app import app
    from fastapi.testclient import TestClient
    client = TestClient(app)
    # 不存在的tx_id应返回404
    resp = client.post("/api/portfolio/transactions/999999/auto-confirm")
    assert resp.status_code == 404
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python3.13 -m pytest tests/test_auto_confirm.py -v`
Expected: FAIL with "ImportError: cannot import name 'get_nav_by_date'"

- [ ] **Step 3: 实现 `get_nav_by_date()`**

在 `backend/db/portfolio.py` 的 `preview_sell` 函数之后新增：

```python
def get_nav_by_date(fund_code: str, nav_date: str) -> float | None:
    """从 fund_nav_history 表查询指定日期的净值。无则返回None。"""
    conn = _get_conn()
    try:
        row = conn.execute("""
            SELECT nav FROM fund_nav_history
            WHERE fund_code = ? AND nav_date = ?
            ORDER BY created_at DESC LIMIT 1
        """, (fund_code, nav_date)).fetchone()
        return row["nav"] if row else None
    finally:
        conn.close()
```

- [ ] **Step 4: 新增自动确认路由**

在 `backend/routers/portfolio.py` 的 `confirm_transaction_api` 之后新增：

```python
@router.post("/api/portfolio/transactions/{tx_id}/auto-confirm")
async def auto_confirm_transaction_api(tx_id: int):
    """自动获取确认日净值并确认交易。"""
    from db.portfolio import get_transaction, get_nav_by_date, confirm_transaction, refresh_holding_price

    tx = get_transaction(tx_id)
    if not tx:
        raise HTTPException(404, "交易记录不存在")
    if tx.get("status") != "pending":
        raise HTTPException(400, "只能自动确认 pending 状态的交易")

    fund_code = tx["fund_code"]
    expected_date = tx.get("expected_confirm_date") or tx.get("transaction_date")

    # 优先查询历史净值
    confirmed_price = get_nav_by_date(fund_code, expected_date)

    # 没有历史净值则获取最新净值（并提示）
    used_latest = False
    if not confirmed_price:
        holding_id = tx.get("holding_id")
        if holding_id:
            nav_data = refresh_holding_price(holding_id)
            if nav_data:
                confirmed_price = nav_data.get("nav")
                used_latest = True
        if not confirmed_price:
            raise HTTPException(502, "无法获取基金净值，请稍后重试或手动填入")

    ok = confirm_transaction(tx_id, confirmed_price)
    if not ok:
        raise HTTPException(500, "确认失败")

    return {
        "ok": True,
        "confirmed_price": confirmed_price,
        "nav_source": "history" if not used_latest else "latest",
        "message": "已使用历史净值确认" if not used_latest else f"未找到 {expected_date} 历史净值，已使用最新净值确认，请核实",
    }
```

- [ ] **Step 5: 确认 `get_transaction` 函数存在**

检查 `backend/db/portfolio.py` 是否有 `get_transaction(tx_id)` 函数。如果没有，在 `list_transactions` 之前新增：

```python
def get_transaction(tx_id: int) -> dict | None:
    """获取单条交易记录。"""
    conn = _get_conn()
    try:
        row = conn.execute("SELECT * FROM portfolio_transactions WHERE id = ?", (tx_id,)).fetchone()
        return dict(row) if row else None
    finally:
        conn.close()
```

- [ ] **Step 6: 运行测试验证通过**

Run: `cd backend && python3.13 -m pytest tests/test_auto_confirm.py -v`
Expected: 2 passed

- [ ] **Step 7: 提交**

```bash
git add backend/db/portfolio.py backend/routers/portfolio.py backend/tests/test_auto_confirm.py
git commit -m "feat(portfolio): 自动获取净值确认交易API"
```

---

## Task 6: 前端 — 确认弹窗自动获取按钮 + 批量确认按钮

**Files:**
- Modify: `frontend/src/api/index.js`（新增 `autoConfirmTransaction()`）
- Modify: `frontend/src/components/PortfolioManagement.vue`

- [ ] **Step 1: 新增 API 函数**

在 `frontend/src/api/index.js` 的 `previewSell` 之后新增：

```javascript
/** 自动获取净值确认交易 */
export function autoConfirmTransaction(txId) {
  return api.post(`/portfolio/transactions/${txId}/auto-confirm`, {}, { timeout: 30000 })
}
```

- [ ] **Step 2: 前端导入**

在 `PortfolioManagement.vue` 的 import 区加入 `autoConfirmTransaction`。

- [ ] **Step 3: 增加"自动获取净值"按钮逻辑**

在 `openConfirmTx` 函数（约3018行）附近新增：

```javascript
const autoConfirmLoading = ref(false)

async function fetchAutoNav() {
  const tx = confirmTxData.value
  if (!tx?.id) return
  autoConfirmLoading.value = true
  try {
    const { data } = await autoConfirmTransaction(tx.id)
    confirmTxPrice.value = data.confirmed_price
    if (data.nav_source === 'latest') {
      showToast(data.message, 'warning')
    } else {
      showToast(`已获取净值 ${data.confirmed_price}`)
    }
  } catch (e) {
    showToast('自动获取净值失败: ' + (e.response?.data?.detail || e.message), 'error')
  } finally {
    autoConfirmLoading.value = false
  }
}
```

- [ ] **Step 4: 确认弹窗模板增加按钮**

在确认弹窗的净值输入框区域（查找 `confirmTxPrice` 输入框），在其下方增加：

```html
<button type="button" class="btn-sm btn-secondary" @click="fetchAutoNav" :disabled="autoConfirmLoading">
  {{ autoConfirmLoading ? '获取中...' : '自动获取净值' }}
</button>
```

- [ ] **Step 5: pending交易列表增加"一键确认所有到期交易"按钮**

在 pending 交易列表区域（查找 `pendingTransactions` 或 `listPendingTransactions` 相关模板），在列表顶部增加：

```html
<button class="btn-sm btn-primary" @click="batchAutoConfirm" :disabled="batchConfirmLoading">
  {{ batchConfirmLoading ? '确认中...' : '一键确认所有到期交易' }}
</button>
```

并在 `<script setup>` 增加：

```javascript
const batchConfirmLoading = ref(false)

async function batchAutoConfirm() {
  batchConfirmLoading.value = true
  try {
    const { data } = await autoConfirmPortfolioTransactions()
    showToast(`批量确认完成：${data.result?.confirmed || 0} 笔成功`, data.result?.failed ? 'warning' : 'success')
    loadData()
  } catch (e) {
    showToast('批量确认失败: ' + (e.response?.data?.detail || e.message), 'error')
  } finally {
    batchConfirmLoading.value = false
  }
}
```

- [ ] **Step 6: 验证前端构建**

Run: `cd frontend && npm run build`
Expected: build 成功

- [ ] **Step 7: 提交**

```bash
git add frontend/src/api/index.js frontend/src/components/PortfolioManagement.vue
git commit -m "feat(portfolio-ui): 确认弹窗自动获取净值+批量确认按钮"
```

---

## Task 7: 后端 — list_transactions 日期筛选

**Files:**
- Modify: `backend/db/portfolio.py`（改造 `list_transactions()`）
- Modify: `backend/routers/portfolio.py`（改造路由）
- Test: `backend/tests/test_transaction_filter.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_transaction_filter.py`：

```python
# backend/tests/test_transaction_filter.py
"""测试交易记录日期筛选。"""
import pytest
from db.portfolio import create_holding, delete_holding, create_transaction, list_transactions


def test_list_transactions_date_range_filter():
    """list_transactions 支持日期范围筛选。"""
    holding_id = create_holding(
        fund_code="110030", fund_name="测试筛选基金",
        shares=1000, cost_price=1.0, current_price=1.0,
    )
    try:
        # 创建3笔交易：2026-01-15、2026-02-15、2026-03-15
        create_transaction(fund_code="110030", transaction_type="buy", amount=500,
                          transaction_date="2026-01-15", holding_id=holding_id, status="confirmed")
        create_transaction(fund_code="110030", transaction_type="buy", amount=500,
                          transaction_date="2026-02-15", holding_id=holding_id, status="confirmed")
        create_transaction(fund_code="110030", transaction_type="buy", amount=500,
                          transaction_date="2026-03-15", holding_id=holding_id, status="confirmed")

        # 筛选 2026-02-01 ~ 2026-02-28 应只返回1笔
        result = list_transactions(fund_code="110030", start_date="2026-02-01", end_date="2026-02-28")
        assert len(result) == 1
        assert result[0]["transaction_date"] == "2026-02-15"

        # 筛选 2026-01-01 ~ 2026-03-31 应返回3笔
        result = list_transactions(fund_code="110030", start_date="2026-01-01", end_date="2026-03-31")
        assert len(result) == 3
    finally:
        delete_holding(holding_id)
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && python3.13 -m pytest tests/test_transaction_filter.py -v`
Expected: FAIL with "unexpected keyword argument 'start_date'"

- [ ] **Step 3: 改造 `list_transactions()`**

修改 `backend/db/portfolio.py` 的 `list_transactions` 函数（约539行），增加 `start_date` 和 `end_date` 参数：

```python
def list_transactions(fund_code: str = None, holding_id: int = None,
                      user_id: str = "default", limit: int = 100,
                      include_system: bool = False, status: str = None,
                      start_date: str = None, end_date: str = None) -> list[dict]:
    """获取交易记录列表。默认不包含系统自动生成的（is_system=1）交易。

    支持按日期范围筛选：start_date/end_date（YYYY-MM-DD，闭区间）。
    """
    conn = _get_conn()
    conditions = ["user_id = ?"]
    params = [user_id]
    if fund_code:
        conditions.append("fund_code = ?")
        params.append(fund_code)
    if holding_id:
        conditions.append("holding_id = ?")
        params.append(holding_id)
    if status:
        conditions.append("status = ?")
        params.append(status)
    if start_date:
        conditions.append("transaction_date >= ?")
        params.append(start_date)
    if end_date:
        conditions.append("transaction_date <= ?")
        params.append(end_date)
    if not include_system:
        conditions.append("(is_system IS NULL OR is_system = 0)")

    where = " AND ".join(conditions)
    params.append(limit)
```

- [ ] **Step 4: 改造 API 路由支持日期筛选**

在 `backend/routers/portfolio.py` 找到 `GET /api/portfolio/{holding_id}/transactions` 路由（约829行），增加 `start_date` 和 `end_date` 查询参数：

```python
@router.get("/api/portfolio/{holding_id}/transactions")
async def list_transactions_api(holding_id: int, limit: int = 100,
                                 start_date: str = None, end_date: str = None):
    """获取某持仓的交易记录，支持日期范围筛选。"""
    from db.portfolio import list_transactions
    return list_transactions(holding_id=holding_id, limit=limit,
                            start_date=start_date, end_date=end_date)
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd backend && python3.13 -m pytest tests/test_transaction_filter.py -v`
Expected: 1 passed

- [ ] **Step 6: 提交**

```bash
git add backend/db/portfolio.py backend/routers/portfolio.py backend/tests/test_transaction_filter.py
git commit -m "feat(portfolio): 交易记录支持日期范围筛选"
```

---

## Task 8: 前端 — pending撤销入口 + 交易记录筛选

**Files:**
- Modify: `frontend/src/api/index.js`（新增 `listTransactionsWithFilter()`，已有 `deletePortfolioTransaction`）
- Modify: `frontend/src/components/PortfolioManagement.vue`

- [ ] **Step 1: 新增 API 函数**

在 `frontend/src/api/index.js` 的 `autoConfirmTransaction` 之后新增：

```javascript
/** 获取交易记录（支持筛选） */
export function listTransactionsWithFilter(holdingId, params = {}) {
  return api.get(`/portfolio/${holdingId}/transactions`, { params })
}
```

- [ ] **Step 2: 前端增加交易记录筛选状态**

在 `PortfolioManagement.vue` 增加筛选状态：

```javascript
const txFilter = ref({
  start_date: '',
  end_date: '',
  status: '',  // '' | 'pending' | 'confirmed' | 'settled'
})
const filteredTransactions = ref([])

async function applyTxFilter() {
  if (!selectedHolding.value?.id) return
  try {
    const params = { limit: 100 }
    if (txFilter.value.start_date) params.start_date = txFilter.value.start_date
    if (txFilter.value.end_date) params.end_date = txFilter.value.end_date
    const { data } = await listTransactionsWithFilter(selectedHolding.value.id, params)
    // 客户端按status二次筛选（后端list_transactions支持status，但路由未暴露）
    filteredTransactions.value = txFilter.value.status
      ? data.filter(t => t.status === txFilter.value.status)
      : data
  } catch (e) {
    showToast('筛选失败: ' + (e.response?.data?.detail || e.message), 'error')
  }
}

function clearTxFilter() {
  txFilter.value = { start_date: '', end_date: '', status: '' }
  applyTxFilter()
}
```

- [ ] **Step 3: 增加撤销交易函数**

```javascript
async function cancelPendingTx(tx) {
  if (!confirm(`确认撤销这笔待确认交易？\n${tx.transaction_type === 'buy' ? '买入' : '卖出'} ${tx.fund_name} ${tx.submitted_amount || tx.submitted_shares || ''}`)) return
  try {
    await deletePortfolioTransaction(tx.id)
    showToast('已撤销待确认交易')
    applyTxFilter()
    loadData()
  } catch (e) {
    showToast('撤销失败: ' + (e.response?.data?.detail || e.message), 'error')
  }
}
```

- [ ] **Step 4: 交易记录列表增加撤销按钮**

在交易记录列表模板中（查找交易记录表格），为 pending 状态的交易增加撤销按钮：

```html
<button v-if="tx.status === 'pending'" class="btn-sm btn-danger" @click="cancelPendingTx(tx)">
  撤销
</button>
```

- [ ] **Step 5: 交易记录区域增加筛选条**

在交易记录列表上方增加筛选条：

```html
<div class="tx-filter-bar">
  <input v-model="txFilter.start_date" type="date" class="input-field sm" @change="applyTxFilter" />
  <span>至</span>
  <input v-model="txFilter.end_date" type="date" class="input-field sm" @change="applyTxFilter" />
  <select v-model="txFilter.status" class="input-field sm" @change="applyTxFilter">
    <option value="">全部状态</option>
    <option value="pending">待确认</option>
    <option value="confirmed">已确认</option>
    <option value="settled">已到账</option>
  </select>
  <button class="btn-sm btn-secondary" @click="clearTxFilter">清除</button>
</div>
```

- [ ] **Step 6: 增加 CSS 样式**

```css
.tx-filter-bar { display: flex; gap: 8px; align-items: center; margin-bottom: 12px; flex-wrap: wrap; }
.tx-filter-bar .input-field.sm { width: auto; min-width: 120px; }
.btn-danger { background: #dc2626; color: white; }
.btn-danger:hover { background: #b91c1c; }
```

- [ ] **Step 7: 验证前端构建**

Run: `cd frontend && npm run build`
Expected: build 成功

- [ ] **Step 8: 提交**

```bash
git add frontend/src/api/index.js frontend/src/components/PortfolioManagement.vue
git commit -m "feat(portfolio-ui): pending交易撤销入口+交易记录筛选"
```

---

## 自我审查

### Spec 覆盖
- ✅ 加仓建议（设计稿3.1）→ Task 1+2
- ✅ 减仓预览（设计稿3.2）→ Task 3+4
- ✅ 自动获取净值确认（设计稿3.3.1）→ Task 5+6
- ✅ 批量确认（设计稿3.3.2）→ Task 6
- ✅ pending撤销入口（设计稿3.3.3）→ Task 8
- ✅ 交易记录筛选（设计稿3.3.4）→ Task 7+8

### 类型一致性
- `get_dca_suggestion(holding_id)` → 返回 dict 含 advice/suggestion ✅
- `preview_sell(holding_id, shares)` → 返回 dict 含 warnings ✅
- `get_nav_by_date(fund_code, date)` → 返回 float|None ✅
- `list_transactions(..., start_date, end_date)` → 兼容旧调用 ✅

### 无 Placeholder
- 所有步骤包含完整代码 ✅
- 所有测试包含实际断言 ✅
