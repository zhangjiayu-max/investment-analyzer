# 数据可视化升级方案 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Dashboard、Portfolio 两个核心页面的数据展示从文字+手绘SVG升级为 ECharts 专业图表，同时为 ValuationHistory 增加估值百分位色带，让系统整体视觉达到专业金融工具水准。

**Architecture:** 基于现有 ECharts 6 + CSS 变量设计系统，新增共享图表配置 composable（统一主题/配色/交互），改造 Dashboard 的 2x2 Grid 为图表驱动，将 Portfolio 的手绘 SVG 替换为 ECharts，ValuationHistory 增加百分位色带标注。

**Tech Stack:** Vue 3 Composition API / ECharts 6 / CSS Custom Properties / Tailwind CSS v4

---

## 现状分析

| 页面 | 当前状态 | 问题 |
|------|---------|------|
| Dashboard | 2×2 卡片，纯文字+百分位进度条 | 无图表，数据感知弱 |
| ValuationHistory | ECharts 双轴折线图（PE/PB + 百分位） | 基本合格，缺百分位色带标注 |
| PortfolioManagement | 手绘 SVG 饼图 + 手绘 SVG 折线图 | 不专业，无交互，222KB 巨型组件 |
| BondMarket | ECharts 双轴图（收益率+温度） | 已达标，本轮不改 |

## 文件结构

### 新建文件

| 文件 | 职责 |
|------|------|
| `frontend/src/composables/useChartTheme.js` | ECharts 主题配置 composable（统一配色、暗色适配、响应式） |
| `frontend/src/components/charts/GaugeChart.vue` | 通用仪表盘组件（市场温度/估值温度） |
| `frontend/src/components/charts/HeatmapChart.vue` | 涨跌热力图组件 |
| `frontend/src/components/charts/PieChart.vue` | 通用饼图/环形图组件 |
| `frontend/src/components/charts/LineChart.vue` | 通用折线图组件（带面积、标记线） |

### 修改文件

| 文件 | 改动范围 |
|------|---------|
| `frontend/src/components/Dashboard.vue` | 模板 L405+ 增加图表卡片；script 增加图表数据处理 |
| `frontend/src/components/PortfolioManagement.vue` | 替换 SVG 饼图为 PieChart；替换 SVG 折线为 LineChart |
| `frontend/src/components/ValuationHistory.vue` | renderTrendChart 函数增加百分位色带 |

---

## Task 1: 创建共享图表主题 composable

**Files:**
- Create: `frontend/src/composables/useChartTheme.js`

- [ ] **Step 1: 创建 useChartTheme.js**

```js
// frontend/src/composables/useChartTheme.js
import { computed } from 'vue'
import { isDark } from './useTheme'

/**
 * ECharts 共享主题配置
 * 统一配色、字体、网格、tooltip 风格
 */
export function useChartTheme() {
  const theme = computed(() => {
    const dark = isDark.value
    return {
      // 基础色彩
      backgroundColor: 'transparent',
      textColor: dark ? '#9aa0a6' : '#64748b',
      titleColor: dark ? '#e8eaed' : '#0f172a',
      gridColor: dark ? 'rgba(255,255,255,0.06)' : '#f1f5f9',
      borderColor: dark ? 'rgba(255,255,255,0.1)' : '#e2e8f0',

      // 金融配色（涨红跌绿，中国市场惯例）
      colors: {
        profit: '#dc2626',     // 红色 = 盈利/上涨
        loss: '#059669',       // 绿色 = 亏损/下跌
        primary: '#c9a84c',    // 暖金色（主色调）
        accent: '#0ea5e9',     // 天蓝色
        warning: '#f59e0b',    // 琥珀色
        danger: '#ef4444',     // 红色
        series: [              // 多系列配色
          '#c9a84c', '#10b981', '#f59e0b', '#ef4444',
          '#8b5cf6', '#06b6d4', '#f97316', '#ec4899',
        ],
      },

      // 百分位色带（估值用）
      percentileBands: [
        { min: 0, max: 20, color: 'rgba(16,185,129,0.15)', label: '极度低估' },
        { min: 20, max: 40, color: 'rgba(16,185,129,0.08)', label: '低估' },
        { min: 40, max: 60, color: 'rgba(156,163,175,0.05)', label: '合理' },
        { min: 60, max: 80, color: 'rgba(245,158,11,0.08)', label: '偏高' },
        { min: 80, max: 100, color: 'rgba(239,68,68,0.12)', label: '高估' },
      ],
    }
  })

  /** 通用 tooltip 配置 */
  function getTooltipOpts(extra = {}) {
    const t = theme.value
    return {
      backgroundColor: isDark.value ? 'rgba(13,18,32,0.95)' : '#ffffff',
      borderColor: t.borderColor,
      borderWidth: 1,
      textStyle: { color: t.titleColor, fontSize: 12 },
      ...extra,
    }
  }

  /** 通用 grid 配置 */
  function getGridOpts(opts = {}) {
    return {
      left: opts.left || '10%',
      right: opts.right || '10%',
      top: opts.top || '8%',
      bottom: opts.bottom || '18%',
      containLabel: true,
    }
  }

  /** 通用 xAxis 配置 */
  function getCategoryAxis(data, opts = {}) {
    const t = theme.value
    return {
      type: 'category',
      data,
      axisLabel: { color: t.textColor, fontSize: 10, rotate: data.length > 15 ? 30 : 0, ...opts.labelOpts },
      axisLine: { lineStyle: { color: t.gridColor } },
      axisTick: { show: false },
      ...opts,
    }
  }

  /** 通用 yAxis 配置 */
  function getValueAxis(name, opts = {}) {
    const t = theme.value
    return {
      type: 'value',
      name,
      nameTextStyle: { color: t.textColor, fontSize: 10 },
      splitLine: { lineStyle: { color: t.gridColor, type: 'dashed' } },
      axisLabel: { color: t.textColor, fontSize: 10 },
      axisLine: { show: false },
      axisTick: { show: false },
      ...opts,
    }
  }

  /** dataZoom 配置 */
  function getDataZoomOpts(dataLen, threshold = 60) {
    const t = theme.value
    const start = dataLen > threshold ? Math.max(0, (1 - threshold / dataLen) * 100) : 0
    return [
      { type: 'inside', start, end: 100 },
      {
        type: 'slider', start, end: 100, height: 20, bottom: 4,
        borderColor: 'transparent',
        backgroundColor: isDark.value ? 'rgba(255,255,255,0.04)' : '#f8fafc',
        fillerColor: 'rgba(201,168,76,0.12)',
        handleStyle: { color: t.colors.primary, borderColor: t.colors.primary },
        textStyle: { color: t.textColor, fontSize: 10 },
      },
    ]
  }

  return { theme, isDark, getTooltipOpts, getGridOpts, getCategoryAxis, getValueAxis, getDataZoomOpts }
}
```

- [ ] **Step 2: 验证文件语法**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/frontend && node -e "import('./src/composables/useChartTheme.js').catch(e => console.error(e.message))"`
Expected: 无语法错误（可能因 import 路径报错，但语法应通过）

- [ ] **Step 3: Commit**

```bash
git add frontend/src/composables/useChartTheme.js
git commit -m "feat: 新增 ECharts 共享主题 composable — 统一图表配色/字体/交互配置"
```

---

## Task 2: 创建 GaugeChart 仪表盘组件

**Files:**
- Create: `frontend/src/components/charts/GaugeChart.vue`

- [ ] **Step 1: 创建 GaugeChart.vue**

```vue
<!-- frontend/src/components/charts/GaugeChart.vue -->
<!-- 通用仪表盘：用于市场温度、估值温度等 0-100 指标 -->
<script setup>
import { ref, watch, onMounted, onUnmounted, computed } from 'vue'
import * as echarts from 'echarts'
import { useChartTheme } from '../../composables/useChartTheme'

const props = defineProps({
  value: { type: Number, default: 0 },
  title: { type: String, default: '' },
  min: { type: Number, default: 0 },
  max: { type: Number, default: 100 },
  unit: { type: String, default: '' },
  /** 分段配色: [{ from, to, color }] */
  segments: {
    type: Array,
    default: () => [
      { from: 0, to: 30, color: '#3b82f6' },
      { from: 30, to: 50, color: '#60a5fa' },
      { from: 50, to: 70, color: '#f59e0b' },
      { from: 70, to: 100, color: '#ef4444' },
    ],
  },
  height: { type: String, default: '200px' },
})

const chartRef = ref(null)
let chart = null
const { isDark, getTooltipOpts } = useChartTheme()

const option = computed(() => ({
  backgroundColor: 'transparent',
  series: [
    {
      type: 'gauge',
      min: props.min,
      max: props.max,
      startAngle: 210,
      endAngle: -30,
      progress: { show: true, width: 18, roundCap: true },
      pointer: { show: true, length: '60%', width: 5, itemStyle: { color: 'auto' } },
      axisLine: {
        lineStyle: {
          width: 18,
          color: props.segments.map(s => [s.to / props.max, s.color]),
        },
      },
      axisTick: { show: false },
      splitLine: { show: false },
      axisLabel: {
        distance: 24,
        color: isDark.value ? '#9aa0a6' : '#64748b',
        fontSize: 11,
        formatter: v => v === props.min || v === props.max ? v : '',
      },
      title: {
        show: !!props.title,
        offsetCenter: [0, '70%'],
        fontSize: 13,
        color: isDark.value ? '#e8eaed' : '#0f172a',
        fontWeight: 600,
      },
      detail: {
        valueAnimation: true,
        fontSize: 28,
        fontWeight: 700,
        color: 'auto',
        offsetCenter: [0, '40%'],
        formatter: v => `${v}${props.unit}`,
      },
      data: [{ value: props.value, name: props.title }],
    },
  ],
}))

function renderChart() {
  if (!chartRef.value) return
  if (chart) chart.dispose()
  chart = echarts.init(chartRef.value, isDark.value ? 'dark' : null)
  chart.setOption(option.value)
}

function handleResize() { chart?.resize() }

watch(() => [props.value, isDark.value], () => {
  if (chart) chart.setOption(option.value)
})

onMounted(() => {
  renderChart()
  window.addEventListener('resize', handleResize)
})

onUnmounted(() => {
  chart?.dispose()
  window.removeEventListener('resize', handleResize)
})
</script>

<template>
  <div ref="chartRef" :style="{ height, width: '100%' }"></div>
</template>
```

- [ ] **Step 2: 验证组件**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无新增类型错误

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/charts/GaugeChart.vue
git commit -m "feat: 新增 GaugeChart 仪表盘组件 — 支持分段配色/暗色适配/动画"
```

---

## Task 3: 创建 PieChart 饼图组件

**Files:**
- Create: `frontend/src/components/charts/PieChart.vue`

- [ ] **Step 1: 创建 PieChart.vue**

```vue
<!-- frontend/src/components/charts/PieChart.vue -->
<!-- 通用饼图/环形图：用于持仓分布、行业配置等 -->
<script setup>
import { ref, watch, onMounted, onUnmounted, computed } from 'vue'
import * as echarts from 'echarts'
import { useChartTheme } from '../../composables/useChartTheme'

const props = defineProps({
  /** [{ name, value, color? }] */
  data: { type: Array, default: () => [] },
  title: { type: String, default: '' },
  /** 环形图内半径比例 (0 = 实心饼图) */
  innerRadius: { type: Number, default: 45 },
  /** 外半径比例 */
  outerRadius: { type: Number, default: 75 },
  /** 是否显示标签 */
  showLabel: { type: Boolean, default: true },
  /** 图例位置: 'right' | 'bottom' | 'none' */
  legendPosition: { type: String, default: 'right' },
  height: { type: String, default: '280px' },
})

const chartRef = ref(null)
let chart = null
const { theme, isDark } = useChartTheme()

const option = computed(() => {
  const colors = theme.value.colors.series
  const seriesData = props.data.map((d, i) => ({
    name: d.name,
    value: d.value,
    itemStyle: { color: d.color || colors[i % colors.length] },
  }))

  const legend = props.legendPosition === 'none' ? { show: false } : {
    show: true,
    orient: props.legendPosition === 'right' ? 'vertical' : 'horizontal',
    [props.legendPosition === 'right' ? 'right' : 'bottom']: 8,
    [props.legendPosition === 'right' ? 'top' : 'left']: 'center',
    textStyle: { color: theme.value.textColor, fontSize: 11 },
    icon: 'circle',
    itemWidth: 8,
    itemHeight: 8,
    itemGap: 8,
  }

  return {
    backgroundColor: 'transparent',
    title: props.title ? {
      text: props.title,
      left: 'center',
      top: 8,
      textStyle: { color: theme.value.titleColor, fontSize: 13, fontWeight: 600 },
    } : undefined,
    tooltip: {
      ...getTooltipOpts(),
      trigger: 'item',
      formatter: p => `<b>${p.name}</b><br/>金额: ¥${p.value.toLocaleString()}<br/>占比: ${p.percent}%`,
    },
    legend,
    series: [{
      type: 'pie',
      radius: [`${props.innerRadius}%`, `${props.outerRadius}%`],
      center: props.legendPosition === 'right' ? ['40%', '50%'] : ['50%', '55%'],
      avoidLabelOverlap: true,
      itemStyle: { borderRadius: 6, borderColor: isDark.value ? '#1e1e22' : '#ffffff', borderWidth: 2 },
      label: {
        show: props.showLabel,
        formatter: '{b}\n{d}%',
        color: theme.value.textColor,
        fontSize: 11,
      },
      emphasis: {
        label: { show: true, fontSize: 13, fontWeight: 'bold' },
        itemStyle: { shadowBlur: 10, shadowColor: 'rgba(0,0,0,0.2)' },
      },
      data: seriesData,
    }],
  }
})

function getTooltipOpts() {
  return {
    backgroundColor: isDark.value ? 'rgba(13,18,32,0.95)' : '#ffffff',
    borderColor: theme.value.borderColor,
    borderWidth: 1,
    textStyle: { color: theme.value.titleColor, fontSize: 12 },
  }
}

function renderChart() {
  if (!chartRef.value) return
  if (chart) chart.dispose()
  chart = echarts.init(chartRef.value, isDark.value ? 'dark' : null)
  chart.setOption(option.value)
}

function handleResize() { chart?.resize() }

watch(() => [() => props.data, isDark.value], () => {
  if (chart) chart.setOption(option.value)
}, { deep: true })

onMounted(() => {
  renderChart()
  window.addEventListener('resize', handleResize)
})

onUnmounted(() => {
  chart?.dispose()
  window.removeEventListener('resize', handleResize)
})
</script>

<template>
  <div ref="chartRef" :style="{ height, width: '100%' }"></div>
</template>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/charts/PieChart.vue
git commit -m "feat: 新增 PieChart 饼图组件 — 支持环形图/自定义配色/暗色适配"
```

---

## Task 4: 创建 LineChart 折线图组件

**Files:**
- Create: `frontend/src/components/charts/LineChart.vue`

- [ ] **Step 1: 创建 LineChart.vue**

```vue
<!-- frontend/src/components/charts/LineChart.vue -->
<!-- 通用折线图：用于收益曲线、净值走势等 -->
<script setup>
import { ref, watch, onMounted, onUnmounted, computed } from 'vue'
import * as echarts from 'echarts'
import { useChartTheme } from '../../composables/useChartTheme'

const props = defineProps({
  /** X 轴日期标签 */
  dates: { type: Array, default: () => [] },
  /** 数据系列: [{ name, data, color?, type?, yAxisIndex? }] */
  series: { type: Array, default: () => [] },
  /** Y 轴名称列表 */
  yNames: { type: Array, default: () => [''] },
  /** 是否显示面积填充 */
  area: { type: Boolean, default: true },
  /** 是否平滑曲线 */
  smooth: { type: Boolean, default: true },
  /** 标记线: [{ yAxis, label, color }] */
  markLines: { type: Array, default: () => [] },
  /** 是否启用 dataZoom */
  zoomable: { type: Boolean, default: true },
  /** dataZoom 阈值（超过此数量自动启用缩放） */
  zoomThreshold: { type: Number, default: 60 },
  height: { type: String, default: '300px' },
})

const chartRef = ref(null)
let chart = null
const { theme, isDark, getTooltipOpts, getGridOpts, getCategoryAxis, getValueAxis, getDataZoomOpts } = useChartTheme()

const option = computed(() => {
  const colors = theme.value.colors.series
  const seriesConfig = props.series.map((s, i) => {
    const color = s.color || colors[i % colors.length]
    return {
      name: s.name,
      type: s.type || 'line',
      yAxisIndex: s.yAxisIndex || 0,
      data: s.data,
      smooth: props.smooth,
      symbol: 'circle',
      symbolSize: 4,
      lineStyle: { width: 2, color },
      itemStyle: { color },
      areaStyle: props.area ? {
        color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
          { offset: 0, color: color.replace(')', ',0.2)').replace('rgb', 'rgba') },
          { offset: 1, color: color.replace(')', ',0)').replace('rgb', 'rgba') },
        ]),
      } : undefined,
      markLine: props.markLines.length ? {
        silent: true,
        symbol: 'none',
        lineStyle: { type: 'dashed', width: 1 },
        data: props.markLines.map(ml => ({
          yAxis: ml.yAxis,
          label: { formatter: ml.label, color: theme.value.textColor, fontSize: 10 },
          lineStyle: { color: ml.color || theme.value.colors.warning },
        })),
      } : undefined,
    }
  })

  const yAxes = props.yNames.map((name, i) =>
    getValueAxis(name, { position: i === 0 ? 'left' : 'right', ...(i > 0 ? { splitLine: { show: false } } : {}) })
  )

  return {
    backgroundColor: 'transparent',
    tooltip: {
      ...getTooltipOpts(),
      trigger: 'axis',
    },
    legend: {
      data: props.series.map(s => s.name),
      bottom: 0,
      textStyle: { color: theme.value.textColor, fontSize: 11 },
    },
    grid: getGridOpts(),
    xAxis: getCategoryAxis(props.dates),
    yAxis: yAxes,
    dataZoom: props.zoomable ? getDataZoomOpts(props.dates.length, props.zoomThreshold) : undefined,
    series: seriesConfig,
  }
})

function renderChart() {
  if (!chartRef.value) return
  if (chart) chart.dispose()
  chart = echarts.init(chartRef.value, isDark.value ? 'dark' : null)
  chart.setOption(option.value)
}

function handleResize() { chart?.resize() }

watch(() => [props.dates, props.series, isDark.value], () => {
  if (chart) chart.setOption(option.value, true)
}, { deep: true })

onMounted(() => {
  renderChart()
  window.addEventListener('resize', handleResize)
})

onUnmounted(() => {
  chart?.dispose()
  window.removeEventListener('resize', handleResize)
})
</script>

<template>
  <div ref="chartRef" :style="{ height, width: '100%' }"></div>
</template>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/charts/LineChart.vue
git commit -m "feat: 新增 LineChart 折线图组件 — 支持多轴/面积填充/标记线/dataZoom"
```

---

## Task 5: Dashboard 增加市场温度仪表盘

**Files:**
- Modify: `frontend/src/components/Dashboard.vue` (script 部分 ~L1-250, template ~L509+)

- [ ] **Step 1: 在 script 中导入 GaugeChart**

在 Dashboard.vue 的 import 区域（L3-6 附近）追加：

```js
import GaugeChart from './charts/GaugeChart.vue'
```

- [ ] **Step 2: 在"每日简报"卡片下方、2x2 Grid 上方插入温度仪表盘**

在 Dashboard.vue 模板中找到 `<!-- 2x2 Grid -->` 注释（约 L509），在其上方插入：

```html
    <!-- 市场温度仪表盘组 -->
    <div v-if="!loading && (marketTemperature || bondTemperature)" class="temp-gauges-row">
      <div v-if="marketTemperature" class="temp-gauge-card card">
        <GaugeChart
          :value="marketTemperature.temperature ?? marketTemperature.degree ?? 0"
          title="A股市场温度"
          :segments="[
            { from: 0, to: 20, color: '#3b82f6' },
            { from: 20, to: 40, color: '#60a5fa' },
            { from: 40, to: 60, color: '#10b981' },
            { from: 60, to: 80, color: '#f59e0b' },
            { from: 80, to: 100, color: '#ef4444' },
          ]"
          height="180px"
        />
        <div class="temp-gauge-label">
          <span class="temp-gauge-desc">{{ marketTemperature.label || marketTemperature.level || '' }}</span>
          <span class="temp-gauge-hint" v-if="marketTemperature.percentile != null">百分位: {{ marketTemperature.percentile }}%</span>
        </div>
      </div>
      <div v-if="bondTemperature" class="temp-gauge-card card">
        <GaugeChart
          :value="bondTemperature.degree ?? 0"
          title="债市温度"
          height="180px"
        />
        <div class="temp-gauge-label">
          <span class="temp-gauge-desc">{{ bondTemperature.label || '' }}</span>
        </div>
      </div>
    </div>
```

- [ ] **Step 3: 在 script 中加载市场温度和债市温度数据**

在 Dashboard.vue 的 `onMounted` 中（约 L114 之后）增加加载逻辑，需要：
1. 在 import 中增加 `getBondMarketTemperature`（如果还没有）
2. 添加 `bondTemperature` ref
3. 在 `onMounted` 的 `Promise.all` 中增加 `loadBondTemperature()`

```js
const bondTemperature = ref(null)

async function loadBondTemperature() {
  try {
    const { data } = await getBondMarketTemperature()
    bondTemperature.value = data?.current || null
  } catch (_) {}
}
```

- [ ] **Step 4: 添加 CSS 样式**

在 Dashboard.vue 的 `<style scoped>` 中追加：

```css
.temp-gauges-row {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1rem;
  margin-bottom: 1rem;
}

.temp-gauge-card {
  text-align: center;
  padding: 0.75rem;
}

.temp-gauge-label {
  margin-top: -0.5rem;
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.temp-gauge-desc {
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--text-primary);
}

.temp-gauge-hint {
  font-size: 0.75rem;
  color: var(--text-tertiary);
}
```

- [ ] **Step 5: 构建前端验证**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/frontend && npm run build:deploy 2>&1 | tail -5`
Expected: 构建成功，无错误

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/Dashboard.vue
git commit -m "feat: Dashboard 增加市场温度/债市温度仪表盘可视化"
```

---

## Task 6: Dashboard 低估指数卡片增加迷你柱状图

**Files:**
- Modify: `frontend/src/components/Dashboard.vue` (template ~L561-577)

- [ ] **Step 1: 将百分位进度条替换为迷你水平柱状图**

在 Dashboard.vue 模板中找到 `index-row` 区域（约 L561），将现有的百分位进度条 `percentile-bar-bg` 替换为更直观的展示。保持现有结构但优化视觉：

```html
          <div v-for="idx in data.undervalued_indexes.slice(0, 10)" :key="idx.index_code" class="index-row" @click="emit('navigate', 'valuation')">
            <div class="index-info">
              <span class="index-name">{{ idx.index_name || idx.index_code }}</span>
              <span class="index-meta">{{ idx.metric_type }} {{ idx.current_value }}</span>
            </div>
            <div class="index-percentile">
              <div class="percentile-bar-bg">
                <div class="percentile-bar" :style="{ width: idx.percentile + '%', background: getPercentileColor(idx.percentile) }"></div>
                <!-- 刻度标记 -->
                <div class="percentile-marks">
                  <span class="mark" style="left:20%">20</span>
                  <span class="mark" style="left:40%">40</span>
                  <span class="mark" style="left:60%">60</span>
                  <span class="mark" style="left:80%">80</span>
                </div>
              </div>
              <div class="percentile-value" :style="{ color: getPercentileColor(idx.percentile) }">
                {{ idx.percentile }}%
              </div>
            </div>
            <span class="assessment-tag" :style="{ background: (assessmentColors[idx.assessment_level] || assessmentColors.fair).bg + '18', color: (assessmentColors[idx.assessment_level] || assessmentColors.fair).bg }">
              {{ idx.assessment }}
            </span>
          </div>
```

- [ ] **Step 2: 添加刻度标记的 CSS**

在 Dashboard.vue 的 `<style scoped>` 中追加：

```css
.percentile-marks {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
}

.percentile-marks .mark {
  position: absolute;
  top: -14px;
  transform: translateX(-50%);
  font-size: 0.6rem;
  color: var(--text-tertiary);
  opacity: 0.5;
}

.percentile-marks .mark::after {
  content: '';
  position: absolute;
  left: 50%;
  top: 100%;
  width: 1px;
  height: 4px;
  background: var(--text-tertiary);
  opacity: 0.3;
}
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Dashboard.vue
git commit -m "feat: Dashboard 低估指数百分位增加刻度标记，增强可读性"
```

---

## Task 7: Portfolio 手绘 SVG 饼图替换为 ECharts PieChart

**Files:**
- Modify: `frontend/src/components/PortfolioManagement.vue` (import 区 + template 中饼图部分)

- [ ] **Step 1: 导入 PieChart 组件**

在 PortfolioManagement.vue 的 import 区域追加：

```js
import PieChart from './charts/PieChart.vue'
```

- [ ] **Step 2: 在 script 中添加饼图数据 computed**

在 `holdingWeights` computed（约 L40）附近追加：

```js
// ECharts 饼图数据格式
const pieChartData = computed(() => {
  if (!holdingWeights.value.length) return []
  return holdingWeights.value
    .filter(h => h.weight >= 0.5) // 过滤掉占比 <0.5% 的小持仓
    .map(h => ({
      name: h.fund_name || h.fund_code,
      value: h.current_value || 0,
    }))
})
```

- [ ] **Step 3: 在模板中找到手绘 SVG 饼图，替换为 PieChart**

在 PortfolioManagement.vue 模板中搜索 `<svg` 或 `pie` 或 `calcPieSlices`，找到手绘饼图的 SVG 代码块，用以下替换：

```html
<PieChart
  v-if="pieChartData.length"
  :data="pieChartData"
  :inner-radius="50"
  :outer-radius="78"
  legend-position="right"
  height="260px"
/>
<div v-else class="chart-empty">暂无持仓数据</div>
```

- [ ] **Step 4: 构建前端验证**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/frontend && npm run build:deploy 2>&1 | tail -5`
Expected: 构建成功

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/PortfolioManagement.vue
git commit -m "feat: Portfolio 手绘 SVG 饼图替换为 ECharts PieChart 组件"
```

---

## Task 8: Portfolio 手绘 SVG 折线图替换为 ECharts LineChart

**Files:**
- Modify: `frontend/src/components/PortfolioManagement.vue` (template 中折线图 + SVG 坐标计算函数)

- [ ] **Step 1: 在 script 中添加折线图数据 computed**

在 PortfolioManagement.vue 中搜索 `chartData` 或 `nav_history`，在相关位置追加：

```js
// ECharts 折线图数据
const navChartData = computed(() => {
  if (!chartData.value?.nav_history?.length) return { dates: [], series: [] }
  const history = chartData.value.nav_history
  return {
    dates: history.map(d => d.date),
    series: [{
      name: '组合净值',
      data: history.map(d => d.nav),
      color: '#c9a84c',
    }],
  }
})
```

- [ ] **Step 2: 导入 LineChart 组件**

在 import 区追加：

```js
import LineChart from './charts/LineChart.vue'
```

- [ ] **Step 3: 在模板中找到手绘 SVG 折线图，替换为 LineChart**

搜索模板中的 `<svg` 和 `polyline` 或 `scaleX`/`scaleY`，找到净值曲线的 SVG 代码，用以下替换：

```html
<LineChart
  v-if="navChartData.dates.length"
  :dates="navChartData.dates"
  :series="navChartData.series"
  :y-names="['净值']"
  :area="true"
  :smooth="true"
  height="280px"
/>
<div v-else class="chart-empty">暂无净值数据</div>
```

注意：需要保留交易标记（买入/卖出点）的功能，如果 SVG 中有交易标记逻辑，可以后续通过 LineChart 的 markPoint 扩展实现。

- [ ] **Step 4: 清理不再需要的 SVG 计算函数**

如果 `scaleX`、`scaleY`、`chartDataYRange`、`polylinePoints` 等函数只用于被替换的 SVG 图表，可以安全移除。但先确认没有其他地方使用。

- [ ] **Step 5: 构建前端验证**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/frontend && npm run build:deploy 2>&1 | tail -5`
Expected: 构建成功

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/PortfolioManagement.vue
git commit -m "feat: Portfolio 手绘 SVG 净值曲线替换为 ECharts LineChart 组件"
```

---

## Task 9: ValuationHistory 增加百分位色带标注

**Files:**
- Modify: `frontend/src/components/ValuationHistory.vue` (renderTrendChart 函数 ~L424-516)

- [ ] **Step 1: 修改 renderTrendChart 增加 markArea**

在 ValuationHistory.vue 的 `renderTrendChart` 函数中，找到 `series` 数组定义（约 L486），在百分位系列中增加 `markArea`：

将 L503-513 的百分位系列替换为：

```js
      {
        name: '分位点(%)',
        type: 'line',
        yAxisIndex: 1,
        data: percentileData,
        smooth: true,
        lineStyle: { width: 2, color: '#f59e0b', type: 'dashed' },
        itemStyle: { color: '#f59e0b' },
        symbol: 'circle',
        symbolSize: 5,
        markArea: {
          silent: true,
          data: [
            [
              { yAxis: 0, itemStyle: { color: 'rgba(16,185,129,0.08)' } },
              { yAxis: 30 },
            ],
            [
              { yAxis: 30, itemStyle: { color: 'rgba(16,185,129,0.04)' } },
              { yAxis: 50 },
            ],
            [
              { yAxis: 50, itemStyle: { color: 'transparent' } },
              { yAxis: 70 },
            ],
            [
              { yAxis: 70, itemStyle: { color: 'rgba(245,158,11,0.04)' } },
              { yAxis: 85 },
            ],
            [
              { yAxis: 85, itemStyle: { color: 'rgba(239,68,68,0.06)' } },
              { yAxis: 100 },
            ],
          ],
        },
      },
```

- [ ] **Step 2: 在图表下方增加百分位图例说明**

在 ValuationHistory.vue 模板中找到 `trendChartRef` 的 div（约 L848），在其下方追加：

```html
        <div class="percentile-legend">
          <span class="legend-item"><span class="legend-color" style="background:rgba(16,185,129,0.15)"></span>低估区 (&lt;30%)</span>
          <span class="legend-item"><span class="legend-color" style="background:rgba(156,163,175,0.08)"></span>合理区 (30-70%)</span>
          <span class="legend-item"><span class="legend-color" style="background:rgba(239,68,68,0.12)"></span>高估区 (&gt;70%)</span>
        </div>
```

- [ ] **Step 3: 添加图例样式**

在 ValuationHistory.vue 的 `<style scoped>` 中追加：

```css
.percentile-legend {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  margin-top: 0.5rem;
  font-size: 0.75rem;
  color: var(--text-tertiary);
}

.percentile-legend .legend-item {
  display: flex;
  align-items: center;
  gap: 0.35rem;
}

.percentile-legend .legend-color {
  display: inline-block;
  width: 12px;
  height: 12px;
  border-radius: 2px;
}
```

- [ ] **Step 4: 构建前端验证**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/frontend && npm run build:deploy 2>&1 | tail -5`
Expected: 构建成功

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/ValuationHistory.vue
git commit -m "feat: ValuationHistory 趋势图增加百分位色带标注（低估/合理/高估区域可视化）"
```

---

## Task 10: 最终构建验证 + 整体 Commit

- [ ] **Step 1: 完整构建**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/frontend && npm run build:deploy 2>&1`
Expected: 构建成功，无警告无错误

- [ ] **Step 2: 启动后端验证页面加载**

Run: `cd /Users/xiaoyuer/projects/investment-analyzer/backend && python3 -m uvicorn app:app --host 0.0.0.0 --port 8000 --reload &`
然后打开浏览器访问 http://localhost:8000 检查：
- Dashboard 是否显示温度仪表盘
- ValuationHistory 趋势图是否有百分位色带
- Portfolio 饼图是否正常渲染

- [ ] **Step 3: 最终 Commit**

```bash
git add -A
git commit -m "feat: 数据可视化升级 Phase 1 完成 — 仪表盘/饼图/折线图/百分位色带"
```

---

## 验收清单

| 检查项 | 预期 |
|--------|------|
| Dashboard 市场温度仪表盘 | 0-100 分段色表盘，暗色/亮色适配 |
| Dashboard 低估指数刻度标记 | 百分位条上有 20/40/60/80 刻度 |
| ValuationHistory 百分位色带 | 趋势图背景有低/中/高估色带 + 底部图例 |
| Portfolio 饼图 | ECharts 环形图，悬停交互，暗色适配 |
| Portfolio 净值曲线 | ECharts 折线图，面积填充，平滑曲线 |
| 暗色模式切换 | 所有图表随主题切换颜色 |
| 响应式 | 图表容器自适应宽度 |
| 构建无报错 | `npm run build:deploy` 成功 |
