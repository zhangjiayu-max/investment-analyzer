# 2026-06-19 工作日志

## 投资分析助手 全面升级 — 阶段一完成（KYC 画像 + 记忆打通）

用户诉求：多 Agent 越用越懂用户，提升对话/分析质量，参考理财专家团队。
决策：全面升级 + 完整 KYC + 单用户（user_id='default'）。
计划文件：/Users/xiaoyuer/.workbuddy/plans/radiant-aurora-darwin.md

### 阶段一交付（已验证通过）
1. **F1 记忆注入打通**（最致命断层）：`backend/agent/memory.py:152` build_user_memory_context 新增第4段，调用 memory_lifecycle.get_memories 读取 user_memories 表（token 预算 300 裁剪，跳过已归档/压缩）。此前该表只写不读。
2. **KYC 画像表扩展**：`backend/db/eval.py` user_profiles 加 10 字段（risk_tolerance/investment_horizon/capital_scale/investment_experience/loss_tolerance/focus_assets/kyc_completed/kyc_completed_at/kyc_version/kyc_source），用 _add_column_if_not_exists 幂等添加。dashboard.py 用 SELECT */**fields 动态读写，无需改。
3. **KYC 问卷后端**：新增 `backend/agent/kyc.py`（6 题问卷 + DIMENSION_META + get_kyc_profile/submit_kyc_answers/update_kyc_dimension/kyc_profile_to_text）；新增 `backend/routers/profile.py`（4 API）；app.py 注册路由。
4. **对话中持续学习**：新增 `backend/agent/kyc_learner.py`（关键词触发 + LLM 提取信号 + 高置信>=0.7 回写）；conversations.py send_message_stream 存用户消息后 asyncio.create_task(asyncio.to_thread(learn_from_message)) 异步触发。
5. **画像注入专家 prompt**：multi_agent.py 新增 _inject_kyc_profile，在 run_specialist 和 run_specialist_with_context 持仓注入后插 <kyc_profile>，按 knowledge_scope.kyc_dimensions 裁剪（未配置则全维度）。
6. **KYC 问卷前端**：新增 KycWizard.vue（Teleport 弹窗）；Sidebar 加"我的投资画像"按钮 + 首次未完成自动引导；api/index.js 加 4 个 profile API。vite build 通过。

### 验证
- 后端 py_compile 全通过；集成测试（系统 python）：KYC 字段创建、画像读写、文本渲染、高置信回写、F1 记忆注入全通过；前端 build 686ms 通过。

### 关键设计决策
- KYC 维度独立通路注入（kyc_profile_to_text），与 feedback_learner 通用偏好职责分离
- 学习留痕复用既有 user_preference_learnings 表，不新建表
- 单用户 user_id='default'，不引入多用户隔离

### 阶段二完成（对话体验升级）
1. **流式 LLM 调用**：llm_service.py 新增 _call_llm_stream（stream=True 生成器，yield {content, reasoning}，兼容 model_extra 提取 reasoning_content，best-effort token 记录）
2. **最终答案流式**：orchestrator.py:1863 主路径改流式，逐 chunk yield reasoning_chunk + answer_chunk，失败回退非流式，最后仍 yield answer 全文（兼容持久化+断线重连）。仲裁路径暂不改。
3. **SSE 转发**：conversations.py 消费者路由加 reasoning_chunk/answer_chunk 分支（仅展示不落库）。simple chat 暂不改流式。
4. **前端流式状态**：useStreamingState.js 加 streamingContent/streamingReasoning + 2 case + onAnswerChunk/onReasoning 回调。
5. **思考面板**：新增 ReasoningPanel.vue（可折叠）；ChatMessage.vue 在 message-bubble 前渲染。
6. **增量拼接**：ChatView.vue onAnswerChunk 增量追加，onAnswer 全文兜底+附加专家结果（3 处路径全升级，兼容回退）。
7. **快捷指令**：ChatInput.vue 加 5 快捷按钮（估值/持仓/解读/配置/体检）。
8. **历史分页**：get_messages + get_messages_api 加 offset（后端支持），前端滚动加载留后续。

验证：前端 vite build 3 次通过；后端 py_compile 全通过。

### 关键约束（阶段二）
- 流式必须保留最终 answer 全文事件（持久化+断线重连依赖）
- answer_chunk/reasoning_chunk 仅展示不落库
- reasoning 回传 LLM 的逻辑与前端流式无关，勿动

### 阶段三完成（分析质量优化）
1. **盈米 MCP 接入**：tools/__init__.py 新增 6 工具（search_news/fund_diagnosis/hot_topics/latest_quotations/diagnose_portfolio/search_funds）+ _execute_tool_impl 分支 + _call_yingmi 封装（错误优雅降级）。验证：YINGMI_API_KEY 已配置，真实数据返回成功。
2. **行情缓存**：market_data.py 加 _market_cache（TTL 5min）；get_market_overview + get_index_current_price 加缓存；修复 bare except 为 logger.warning。
3. **RAG 个性化重排**：rag.py rerank_results 加 user_id + _apply_personalization_boost（关注品种 +0.3/词，rag_logs 历史主题 +0.1/词上限0.5）；调用方传 user_id="default"。
4. **文章结构化提取**：article_reader.py 新增 extract_article_structure（LLM 提取核心观点/标的/操作建议/时效性/偏见）；集成到 _fetch_article 返回 structure 字段。
5. **估值融合层**：新增 valuation_fusion.py（fusion_score：LLM 结合基本面/情绪/画像二次判断 + evidence_chain 溯源）；analyze_stock 加 enable_fusion 可选参数（默认 False，批量任务不启用避免成本爆炸）。

验证：所有文件 py_compile 通过；盈米真实数据返回；行情缓存机制测试通过。

### 阶段四完成（理财专家团队重构）
1. **新增 3 个理财专家**：db/agents.py _init_wealth_specialists 初始化 wealth_advisor（专属理财顾问，全维度画像）/behavior_coach（行为金融辅导师）/macro_strategist（宏观策略师）为编排专家（is_specialist=1），分配 tools + kyc_dimensions。验证：3 专家正确加载。
2. **主动关怀预警**：新增 agent/wealth_advisor.py（generate_proactive_alerts：持仓回撤超亏损承受度 + 估值极端区间）+ /api/profile/alerts。验证：扫描真实持仓检出 9 条浮亏超 15% 预警。
3. **需求澄清路由更新**：需求澄清专家 prompt 加入 macro_strategist/behavior_coach/wealth_advisor 路由规则，新专家可被选中参与协作。
4. **最终答案 KYC 注入**：orchestrator 主路径最终答案 messages 注入 KYC 画像，综合输出"懂用户"（轻量主控综合层）。

验证：所有文件 py_compile 通过；3 专家加载成功；主动关怀预警真实数据检出 9 条。

### 后续增强（未做，低优先级）
- 多轮澄清深度升级（信息不足时 wealth_advisor 主动追问最多2轮）- 高风险，留后续
- 完整主控综合层（专家结论回流 wealth_advisor 做最终输出）- 已用 KYC 注入轻量替代
- Agent 表合并（agents + analysis_agents）- 二期

=== 全面升级四阶段全部完成 ===
